# Infinite Canvas 定制版｜项目制作状态

> 最后更新：2026-08-17
> 实际仓库：`F:\qianse-Canvas\Infinite-Canvas`
> 维护者：`qianse70`
> 用途：新会话接手、版本发布前检查、记录已完成事项与后续计划。

## 一、项目背景

本项目基于 `hero8152/Infinite-Canvas` 二次开发，目标不是重做一套全新画布，而是在保留原项目生成能力的基础上，把普通画布和智能画布改造成更适合非技术用户长期使用的本地 AI 创作工具。

当前定制仓库与上游已经分开：

| 项目 | 地址 / 分支 | 作用 |
|---|---|---|
| 定制仓库 `origin` | `MAOMAOCHONGNE/Infinite-Canvas` / `my-custom` | 日常开发、发布和用户更新来源 |
| 原作者仓库 `upstream` | `hero8152/Infinite-Canvas` / `main` | 查看原作者更新，不能直接覆盖定制版 |

程序内的更新功能只检测和下载用户自己的 GitHub `my-custom` 分支与 ModelScope `master` 国内镜像，不会回退到原作者版本。更新时优先使用用户选择的源，失败后才自动切换到另一个用户自己的源。用户通过 ZIP 下载项目后，即使没有安装 Git，只要项目文件完整、任一更新源可访问，仍可使用程序内更新功能。

## 二、核心目标

1. **让普通用户能看懂、敢操作**：减少隐晦术语，统一中文文案，重要操作提供提示和可视反馈。
2. **让普通画布和智能画布体验尽量一致**：快捷键、循环、提示词、图片编辑、资产库等能力逐步对齐。
3. **让复杂流程可预测**：运行前能看见执行范围，循环失败时不中断整批任务，避免重复付费。
4. **让数据可迁移、可恢复**：支持项目、画布、平台配置、RunningHub、提示词库的选择性备份；密钥不进入备份。
5. **让定制更新不再被上游覆盖**：定制仓库独立维护，更新前自动备份，更新范围与用户数据分离。
6. **让第三方 API 尽可能兼容**：支持 OpenAI、异步、Gemini、方舟、RunningHub、CLI 等多种协议，同时保留供应商特殊模式。
7. **控制本地存储和长期维护成本**：素材分类、孤立素材识别、缩略图、测试用例和状态文档都要能持续维护。

## 三、项目结构与数据边界

| 路径 | 主要内容 | 维护注意事项 |
|---|---|---|
| `main.py` | FastAPI 后端、API 调用、更新、备份、素材与画布服务 | 核心后端文件，发布前必须做编译和接口回归 |
| `static/canvas.html` / `static/js/canvas.js` | 普通画布 | 当前定制最多的区域之一 |
| `static/smart-canvas.html` / `static/js/smart-canvas.js` | 智能画布 | 需防止普通画布改动破坏智能画布原逻辑 |
| `static/api-settings.html` | API 平台、协议和图片接口模式设置 | 模式名字相近，后续需补充解释 |
| `static/asset-manager.html` / `static/js/asset-manager.js` | 角色库、工作流、提示词库、画布资产、本地素材 | 与本地文件和用户数据关系紧密 |
| `backup_transfer.py` | 备份导入、导出和安全过滤 | ZIP 发布时必须包含，否则启动会报模块缺失 |
| `data/` | 画布、配置、工作流、提示词、用户索引等 | 多数属于用户数据，不能在更新时粗暴覆盖 |
| `assets/`、`output/` | 上传素材、生成结果、缩略图等 | 体积可能很大，删除前必须检查引用关系 |
| `API/.env` | API Key、Token 等本机秘密 | 绝不能提交、导出或写进状态文档 |
| `tests/` | Node 与 Python 自动测试 | 发布前必须运行；目前存在一组旧 Python 测试债 |

程序自动更新目前允许覆盖 `main.py`、`VERSION`、说明文档、启动/停止脚本和 `static/`，不会把 `API/.env`、`data/`、`assets/`、`output/` 当作普通更新文件覆盖。

## 四、当前仓库状态

### 4.1 Git 与版本

| 项目 | 当前值 |
|---|---|
| 当前分支 | `my-custom` |
| 跟踪分支 | `origin/my-custom` |
| 最近已同步提交 | `6021866` — `feat: add updater download progress`（2026-08-17） |
| 用户可见版本 | `2026.08.17-custom.2`（来自根目录 `VERSION`） |
| 更新说明 | `新增更新下载进度显示` |
| 更新源 | `MAOMAOCHONGNE/Infinite-Canvas` 的 `my-custom` 分支 |
| ModelScope 更新源 | `qisese70/Infinite-Canvas` / `master`，作为国内镜像与 GitHub 双源备用 |

`main.py` 里仍留有 `APP_VERSION = "2026.08.12-custom.1"`，但当前没有被业务代码引用。实际接口会优先读取根目录 `VERSION`，所以用户看到的是 `2026.08.17-custom.1`。这个旧常量不影响当前显示，但容易误导维护者，后续应删除或改为与 `VERSION` 同步。

### 4.2 当前未提交内容

| 文件 | 类型 | 建议 |
|---|---|---|
| `API/.env` | 本机秘密配置 | 永远不要提交；只保留在用户电脑 |
| `data/asset_library.json` | 用户素材库数据 | 默认不要跟代码一起提交，除非明确要发布默认素材 |
| `static/canvas.html` | 普通画布脚本缓存版本号变化 | 当前用于加载 Alt 拖动连线修复，发布时应保留较新的数字缓存号 |
| `static/js/canvas.js` / `tests/canvas_alt_drag.test.cjs` | 普通画布 Alt 拖动复制外部入站连线及回归测试 | 与其他未提交画布改动一起审查，不要整体回退 |
| `main.py` / `static/index.html` / `tests/test_updater_progress.py` / `tests/updater_progress_ui.test.cjs` | 更新下载实时进度、阶段、速度、耗时与 ETA | 已在隔离 3013 服务完成桌面和窄屏验证，发布时需一并保留 |
| `main.py` / `static/online.html` / `static/js/i18n/studio.js` / `tests/*online_runninghub_params*` | 在线生图 RunningHub 参数自适应 | 纯图片应用不再强制提示词，应用/工作流显示真实参数并通过字段白名单提交；已在隔离 3015 服务完成桌面和窄屏验证 |
| `main.py` / `static/js/canvas.js` / `static/js/i18n/canvas.js` / `tests/*runninghub_cancel*` | 普通画布 RunningHub 官网任务远程取消 | 应用、工作流和 MiniMax RunningHub 工作流支持按节点/流程取消已提交任务；模型 API `/openapi/v2` 不套用 ComfyUI 取消接口 |
| `static/api-settings.html` / `static/js/api-settings.js` / `static/css/api-settings.css` | RunningHub 回收站、可恢复删除与普通备份过滤 | 发布时需连同新回归测试一起保留 |
| `tests/test_runninghub_recycle_bin.py` / `tests/runninghub_recycle_bin.test.cjs` | 回收站、工作流存储和旧备份过滤测试 | 新增文件，建议纳入正式提交 |
| `design-qa.md` | 区域框功能验收记录 | 可保留并纳入文档，也可在确认后单独提交 |
| `el.click()` | 浏览器调试语法错误残留 | 建议确认无用后删除，不应进入正式发布 |
| `.planning/2026-08-15-project-status/` | 本次状态整理工作记录 | 可保留为维护记录 |
| `项目制作状态.md` | 本状态文件 | 建议纳入仓库并持续更新 |

### 4.3 自动验证结果

| 检查 | 结果 | 状态 |
|---|---|---|
| Node 自动测试 | 350 / 350 通过 | 通过 |
| `main.py` 编译 | 通过 | 通过 |
| `backup_transfer.py` 编译 | 通过 | 通过 |
| RunningHub 回收站与备份聚焦测试 | Python 16 / 16 通过 | 通过 |
| GPT Image / Banana / 比例聚焦测试 | Python 26 / 26 通过 | 通过 |
| ModelScope 更新器与进度测试 | Python 6 / 6 通过 | 通过 |
| Python unittest | 96 / 96 通过 | 通过 |

过时的日志媒体删除测试已经移除，并改为日志保留规则测试。普通画布与智能画布的生成日志保持只读，每个画布独立保留最新 500 条；日志淘汰不会删除图片、视频或音频文件。后端保存顺序已与前端的“最新在前”保持一致。

## 五、本次长期会话完成的工作

以下按功能归类，记录的是本轮长期会话截至 2026-08-16 已经实现或已经有自动测试覆盖的内容。

### 5.1 定制版发布与更新安全

- 定制版更新来源改为 `MAOMAOCHONGNE/Infinite-Canvas` 的 `my-custom` 分支。
- 保留原作者主页入口，但定制主页、更新通道与原作者更新来源分离。
- 简化主页介绍，定制主页改为浅色视觉。
- 更新前创建备份，更新后支持自动重启与回滚。
- 新增导出备份 / 导入备份：可选择项目、画布、画布素材、API 平台列表、RunningHub 应用/工作流和提示词库。
- API Key、Token、密码、密钥预览等秘密永远不进入备份。
- 备份导入的普通 API 平台按“平台 ID + 标准化请求地址”识别；同 ID 不同地址会自动新增下一个平台 ID，并同步修正导入画布中的平台引用，不覆盖本机平台或密钥。
- RunningHub 备份携带非秘密请求地址；选择任一 RunningHub 应用/工作流时始终采用备份地址，地址相同保留本机普通密钥与钱包密钥，地址不同则在导入成功后清空两者并提示重新填写。
- 备份导入新增可选“界面与使用偏好”：主题、UI 缩放和普通画布常用节点排序可单独导入；新用户默认白天模式保持不变。
- 备份选择树支持滚动和分区折叠，适应大量项目、画布和 RunningHub 工作流。
- 修复干净下载后缺少 `backup_transfer.py` 导致程序无法启动的问题；该文件现在已纳入仓库。
- `run.bat` 改为 `启动服务.bat`，新增 `停止服务.bat` 与 PowerShell 停止脚本。
- 启动时显示本机地址与局域网地址，并用醒目颜色展示。
- 停止服务逻辑尽量只结束当前 Infinite Canvas 的 `python.exe main.py`，避免误杀其他 Python 程序。
- 建立用户自己的公开 ModelScope Studio 国内镜像：`https://modelscope.cn/studios/qisese70/Infinite-Canvas`，其 `master` 与 GitHub 定制分支保持同一发布树。
- 更新器同时检测 GitHub 与 ModelScope；更新弹窗提供两个可选源并记住选择，优先源失败时只在两个用户源之间兜底。
- ModelScope 更新通过公开文件 API 下载，树清单与每个文件都经过白名单过滤；`API/.env`、`data/`、`assets/`、`output/` 不会被更新覆盖。
- 更新弹窗新增实时进度：显示更新阶段、已完成/总文件数、已下载/总大小、下载速度、已用时间、预计剩余时间与当前文件。GitHub 和 ModelScope 均使用分块下载统计；远端不提供总大小时明确显示“计算中”，不伪造 ETA。更新开始后自动收起源列表，桌面与窄屏首屏都能直接看到进度；关闭弹窗只隐藏界面，不会中断后端更新。

### 5.2 画布历史、复制与基础操作

- 普通画布与智能画布接入撤销 / 重做历史。
- 普通画布与智能画布空闲时使用普通箭头；只有按住画布空白区域拖动视口时才显示抓紧手势，节点、图片和编辑工具的专用光标保持不变。
- 修复 `Ctrl+Z`、`Shift+Ctrl+Z` 焦点与历史冲突。
- 修复 Alt + 拖动多选节点时只移动一个副本的问题；现在整组选中节点一起复制和移动。
- 普通画布框选一段流程后使用 `Alt + 左键拖动`，副本会保留选区内部连线，并继续连接原来的未选中前置节点；不会复制通向未选中后续节点的出站连线。`Ctrl+C / Ctrl+V` 仍只复制选区内部连线，不会自动接回原流程。
- 普通画布右上角增加快捷键帮助，并支持 `A` 打开资产库。
- 普通画布与智能画布的生成日志保持只读，不提供删除日志或清理媒体按钮；每个画布只保留最新 500 条成功/失败记录，超过上限时仅淘汰最旧的日志数据，不删除生成文件。
- 普通画布 IMAGE 节点右上角关闭按钮改为删除完整节点及连接；Output 仍保留“清空结果”的原语义。
- 选中已有 IMAGE 节点后按 `Ctrl+V`，新图片会替换当前节点；未明确选中时仍创建新节点。
- API 节点直接连接 API 节点时，自动创建或复用中间 Output，使数据流更直观。

### 5.3 比例、裁剪、扩图、遮罩与图片比较

- “拉伸适配”保留旧的 source 模式，限定到 `1:1`、`1:4`、`1:8`、`2:3`、`3:2`、`3:4`、`4:1`、`4:3`、`4:5`、`5:4`、`8:1`、`9:16`、`16:9`、`21:9` 并按最接近比例临时拉伸参考图；新增“自适应”模式不拉伸参考图且不发送结构化画面比例。
- 拉伸适配的可见比例保持白名单标准名称；例如命中 `21:9` 时显示 `21:9`，不再回填为数学上等价但难以辨认的 `7:3`。内部尺寸计算仍可约分。
- 仅在启用匹配比例时临时拉伸参考图；原始素材与下载文件不被永久改变。
- 普通与智能画布的裁剪比例扩展为：`1:1`、`2:3`、`3:2`、`3:4`、`4:3`、`4:5`、`5:4`、`9:16`、`16:9`、`21:9`、`9:21`。
- 普通与智能画布扩图支持自由扩图、按比例扩图和自定义背景色，默认中灰色。
- 普通画布图片对比增加“默认对比 / 铺满对比”；铺满只改变预览，不修改下载原图。
- 遮罩预览支持自定义颜色与 10%–100% 透明度，默认红色 50%；最终导出的二值遮罩不受预览颜色和透明度影响。

### 5.4 循环节点与完整流程

- 普通循环节点的外观、提示词行、摘要、自动高度和连接体验向智能画布靠齐。
- 图片或提示词连入循环节点时自动打开对应开关；连接移除后会重新判断并关闭无输入的图片开关。
- 循环节点支持反向拖线快速创建上游节点；组与循环节点可以互相出现在兼容的快速创建菜单中。
- “起始轮、每轮图数、总轮数”语义重新整理，图片批次与提示词轮次分开计算。
- 循环摘要显示本轮 / 下一轮图片范围、识别到的提示词数量和本轮提示词预览；已跳过图片半透明显示。
- 循环提示词支持多行本地提示词，每轮依次使用一条，不自动重复；用完后剩余轮次跳过。
- 支持两种运行方式：
  - **运行本循环**：使用已有上游结果，只运行当前循环阶段。
  - **运行完整流程**：先运行循环前节点一次，再依次执行每个循环阶段。
- 多个循环串联时按阶段执行：前一个循环全部完成后，下一个循环才开始。
- 上游生成数量不足时，不再复制最后一张图片；空的尾部轮次跳过，最后一轮图片不足一批时允许按实际数量运行。
- 普通失败不会让整批任务立即停止；取消操作仍会传播并停止任务。
- 失败位置会保留槽位，避免后续循环把图片与提示词错位。
- 循环节点显示失败重试栏，支持“仅重试失败项”，并继续处理后续阶段。
- 鼠标停留运行按钮时，节点与连接线会显示本次执行范围，区分当前 API、当前循环和完整流程。

### 5.5 LLM 节点

- 修复智能画布 LLM 点击运行无反应的问题。
- 普通画布 LLM 支持图片 / 视频输入和缩略图显示。
- 上游输入、循环起始位置或批次变化时，普通画布 LLM 的缩略图和 INPUT 会立即刷新，不需要先运行模型。
- LLM 输出按输入组合记忆；切换到旧输入时恢复对应旧结果，不自动重新收费运行。
- LLM 后台任务状态与结果持久化；返回画布列表或刷新后不会轻易丢失运行结果。
- 普通画布 LLM 重新排版：默认打开 LLM 模式，保留独立聊天模式，系统提示词放在底部，服务商和模型宽度统一。
- LLM 模式与聊天模式的输出记录分离，避免聊天结果覆盖 LLM 输出。
- 模板库、分隔符、系统提示词开关和中文文案向智能画布靠齐。

### 5.6 API 生成与 RunningHub

- 每个图片模型的参数策略现在支持 `自动 / GPT Image / Banana / 传统`；标准 `gpt-image-2` 和 Banana 名称可自动识别，供应商特殊模型 ID 可手动覆盖。
- GPT Image 选择“自适应”时发送 `size:auto` 并省略 `aspect_ratio`；开启分辨率模型映射时仍使用 1K/2K/4K 选择对应的真实模型。
- 普通 API 生成节点默认分辨率由 4K 改为 2K；旧画布保存的 4K 不被强行改写。
- 清晰度显示为“自动 / 低 / 中 / 高”，请求值仍保持兼容参数。
- 张数控件使用与旁边下拉框一致的 UI，预设 `1、2、4、9`，允许手动输入，最大 10 张。
- API 节点内新增本地提示词输入，连接上游提示词时按“上游在前、本地追加在后”组合。
- API 节点提示词标题根据连接状态显示“提示词”或“补充提示词（追加）”，并可直接打开提示词库。
- RunningHub 节点始终展示已连接提示词；当前工作流没有 Prompt/Text 参数时提前提示“不需要提示词输入”。
- 工作流以后出现 Prompt/Text 字段时自动映射并提交，不需要再改节点 UI。
- RunningHub 应用和工作流支持单项或整组导入导出、拖动排序和重复项覆盖 / 跳过策略；缩略图和密钥不进入便携配置。
- API 设置页新增 RunningHub 回收站：普通删除会保留完整应用/工作流配置与工作流 JSON，可单项恢复、彻底删除或清空回收站。
- 内置项目彻底删除后仅保留不可见的最小抑制标记，避免程序更新后重新出现；回收站不会展示这类内部标记。
- 普通备份预览和 ZIP 只包含正常可见的 RunningHub 项目，不再把 `hidden:true` 删除记录计为普通应用/工作流；旧备份导入也会忽略这些隐藏记录。
- 真实旧备份经新规则检查后显示 5 个可见应用、3 个可见工作流，不再误报为 6 个应用、12 个工作流。
- 备份导入完成提示现在按实际合并结果统计新增/覆盖内容，单独显示因冲突跳过的 API 平台、RunningHub 应用/工作流和提示词库；默认冲突策略改为采用备份配置并保留本机密钥，确认提示后通过带时间戳的地址自动刷新当前页面，避免普通刷新命中旧缓存。
- `/api/providers` 现在明确禁止缓存，API 设置页加载平台时也使用 `no-store`；旧版无缩略图的内置 RunningHub 项目会标记为 `builtin`，彻底删除后写入 `purged` 抑制标记，避免旧记录被重新补回回收站。
- 在线生图选择 RunningHub 应用/工作流时会读取条目的真实字段：无提示词字段的放大、抠图类应用自动隐藏 Prompt；通用 1K/2K/4K 与比例控件不再冒充应用参数；下拉、滑块、数字、布尔和文本字段按配置动态显示。后端只接受当前条目已启用的非媒体字段，并按 `nodeId::fieldName` 白名单提交，图片仍来自参考图片槽位。
- 普通画布 RunningHub 应用、工作流和 MiniMax RunningHub 工作流会登记已接受的 `taskId`。点击节点取消、停止完整流程或删除运行中的节点时，后端使用当前配置的 `.cn` / `.ai` 地址及该任务对应的普通 Key / 钱包 Key 调用官网 `/task/openapi/cancel`；`code=807` 按“任务已结束或不存在”处理。
- RunningHub 提交请求不会随本地停止直接中断；如果用户在等待提交结果时取消，画布拿到迟到的 `taskId` 后会立即请求官网取消。因 `TASK_QUEUE_MAXED` 尚未提交的本地排队项只移出队列，不调用远程取消；排队重试期间取消也不会误删后续队列任务。
- 远程取消失败不会阻塞本地停止，界面会明确提示“官网任务可能仍在运行”。同一个 `taskId` 的取消请求在当前页面会去重。RunningHub 模型 API `/openapi/v2` 暂不使用该 ComfyUI 取消接口。

### 5.7 提示词节点与提示词库

- 普通提示词节点加入与智能画布一致的分隔符能力，默认分隔符为 `----`。
- 循环节点能正确识别分隔后的多段提示词，并显示实际上游文本，不再只显示“循环节点 1x”。
- 全局快捷键 `T` 可打开提示词库；非画布页面也能以浏览 / 复制模式打开。
- 模板库可写入 Prompt、API 本地提示词或 LLM 目标节点；无目标时仍可浏览和复制。
- 提示词库支持缩略图：拖入、选择文件、`Ctrl+V` 粘贴；新建未保存模板时先暂存图片，保存后再上传。
- 修复粘贴缩略图时清空未保存名称 / 内容、同时把图片粘到画布的问题。
- 左侧卡片缩略图、标题宽度与右侧 240×200 预览重新布局。
- 分类支持拖动排序并显示明确的插入位置；提示词支持分类内上下排序和拖入其他分类。
- 素材管理页和快捷键 `T` 弹出的提示词库共用缩略图上传、粘贴和即时同步逻辑。
- “模板库”面向用户的显示逐步改为更直观的“提示词”。

### 5.8 图片引用 `@图片`

- 普通 API 生成节点加入图片引用功能。
- 点击下方图片可插入引用，也保留键盘输入 `@` 选择图片的方式。
- 引用显示为“缩略图 + 图片 N”，真实文件名仅在鼠标停留时显示。
- 多张图片可以分别引用，并保持稳定的内部标记；未引用图片仍保持旧版全部传入的兼容行为。
- 当前功能已覆盖普通 API 节点；LLM 聊天每轮独立图片和提示词节点的图片引用仍可继续增强。

### 5.9 素材库、孤立素材与画布封面

- “图片资产”改名为“角色库”，“默认资产库”改名为“角色素材库”。
- 普通画布支持把拖入图片和生成结果保存到当前资产分组。
- 本地素材支持新建真实文件夹分组。
- 新增孤立素材分类：扫描没有被现有画布、回收站画布、角色库或本地素材引用的本地文件，删除前必须确认。
- 素材批量选择统一为“全部选择 / 取消选择”，移除语义重复的“清空”。
- 画布列表支持自动封面、选择画布内图片作为封面、上传自定义封面。
- 修复新建画布卡片遮挡输入框和封面区域误触发图片拖拽的问题。

### 5.10 区域框、便签与常用节点

- 普通与智能画布支持选择异构节点后进行整理、放入区域框、批量下载媒体。
- 区域框位于节点和连线底层，支持标题字号、颜色、删除框、拖动成员和成员自动进出框。
- 新增便签节点：默认大字号、护眼色背景、白色文字，并支持字号和颜色调整。
- 普通画布空白处双击打开节点菜单；原右键长菜单得到简化。
- 新增“常用节点”设置：可勾选、拖动排序、恢复默认。
- 双击菜单与拖线菜单共用同一套常用节点配置；拖线菜单还会排除连接方向不兼容的节点。
- 修复常用节点设置窗口过大、滚轮缩放到背景画布、拖动排序松手回原位等问题。

## 六、关键技术决策

1. **RunningHub 与通用 API 分开处理**：RunningHub 工作流参数映射不应被 GPT / Banana 的图片参数改动影响。
2. **“图片接口模式”是传输封装，不是模型能力表**：切换模式会改变端点、请求体、上传方式或轮询方式，但不会自动把“2K”翻译成某个模型独有的字段。
3. **第三方 API 不仅靠平台名判断模型**：平台和模型名都可能自定义，最终应允许每个模型配置兼容策略，名称规则只能作为自动默认值。
4. **循环输入不自动重复**：图片或提示词不足时跳过剩余轮次，避免悄悄重复旧内容产生错误结果和额外费用。
5. **网络失败重试要考虑扣费**：GPT-Image-2 编辑接口在连接断开时不会盲目自动重试，避免上游已经扣费却再次提交。
6. **用户数据与程序更新分离**：代码更新不能默认覆盖 `data/`、`assets/`、`output/` 和 `.env`。
7. **预览效果与导出数据分离**：遮罩颜色、图片铺满比较等只影响界面预览，不修改原始输出。
8. **模型参数策略以自动识别加手动覆盖为准**：标准模型名走自动策略；供应商改名时由逻辑模型上的 `GPT Image / Banana / 传统` 覆盖决定请求合同，分辨率映射只负责选择最终真实模型。
9. **RunningHub 删除分为回收与抑制两层**：用户可恢复的项目保存完整配置并标记 `recycled`；彻底删除的内置项目只保留 `purged` 抑制 ID。两者都不能进入普通备份。

## 七、当前图片接口模式的真实含义

| 界面选项 | 内部值 | 当前行为 |
|---|---|---|
| 图片：OpenAI 标准 | `openai` | 文生图通常走 `/v1/images/generations` JSON；有参考图时走 `/v1/images/edits` multipart |
| 图片：OpenAI JSON | `openai-json` | 始终走 generations JSON，并把参考图放入 `extra_body.image` |
| 图片：OpenAI 中转 | `openai-video-proxy` | 通过中转式 `/v1/videos` 提交并轮询任务，名称虽叫“图片中转”，实际沿用异步视频式封装 |
| 图片：OpenAI RS | `openai-responses` | 走 `/v1/responses`，使用 `image_generation` 工具 |
| 图片：土豆 GPT-Image-2 异步 | `tudou-async` | 土豆平台专用的异步提交和任务轮询，仅针对相应 GPT-Image-2 模型 |

这些选项解决的是“请求怎么发、结果怎么取”，模型参数策略则负责把同一传输模式转换为 GPT Image、Banana 或传统参数。两者现在分开配置，避免用接口模式猜测 1K/2K/4K 和比例字段。

## 八、尚未完成 / 当前卡点

### P0：发布前必须处理

1. **整理工作区**：确认 `static/canvas.html` 的缓存版本号是否保留；确保 `API/.env` 和用户 `data/asset_library.json` 不进入提交。
2. **清理调试残留**：确认后删除 `el.click()`；决定 `design-qa.md` 是否正式纳入仓库。
3. **干净环境验证**：从 GitHub ZIP 和全新目录各测试一次安装依赖、启动、程序内更新和备份导入。
4. **发布前检查敏感信息**：检查 Git 暂存区而不只是工作区，防止 `.env`、密钥、个人素材或客户画布被提交。

### P1：当前最重要的功能问题

- GPT Image / Banana 参数策略和自动测试已经完成；下一步需要使用用户当前中转平台各做一次低风险实测，核对后台日志中的最终模型、`size`、`image_size` 与 `aspect_ratio`。
- GPT Image 使用 `size:auto` 时由模型决定实际像素尺寸，提示词比例通常接近但不承诺数学上完全精确；界面不能把它描述成严格 2K 输出。

### P2：可选增强

- 智能画布补齐普通画布的图片比较模式（若后续仍需要）。
- 宫格拆分线支持鼠标拖动调整分割位置；当前只完成了遮罩颜色和透明度。
- LLM 聊天模式支持每一轮独立保存所引用图片。
- 提示词节点支持把文字与图片引用一起传给后续 API / LLM。
- 为图片接口模式增加一段用户能看懂的帮助说明，避免仅凭名字试错。
- 对超大视频 / 音频批量下载增加大小预估和进度提示。

## 九、遇到的卡点与踩过的坑

1. **浏览器强刷不等于清除画布状态**：某些快捷键问题来自旧画布保存状态；新建画布正常，说明不能一看到无痕模式正常就认定是静态缓存。
2. **静态资源缓存号很关键**：JS/CSS 已修改但 HTML 版本号未更新时，浏览器可能继续加载旧文件；反向回退缓存号也会造成混乱。
3. **原生拖放容易“松手回原位”**：排序必须在 `dragover` 持续保存最后有效目标，不能在 `dragleave` 过早清空。
4. **弹窗滚轮事件会冒泡到画布**：模态窗口需要阻止 wheel 事件，否则用户滚动列表时背景画布会缩放。
5. **缩略图不是配置 JSON 的一部分**：只复制工作流或 API 配置文件，不一定带走缩略图；备份必须显式处理关联文件。
6. **API 配置有主数据与静态副本**：`data/api_providers.json`、RunningHub 工作流数据和 `static/runninghub/api_providers.json` 用途不同，不能只看文件名认为三者等价。
7. **不要提交 `.env`**：导出备份已排除密钥，但 Git 提交仍需单独防护。
8. **ZIP 发布也要包含后端模块**：之前遗漏 `backup_transfer.py`，导致新电脑启动时报 `ModuleNotFoundError`。
9. **Windows 批处理编码容易乱码**：中文 `.bat` 需要与命令行代码页配合；复杂进程识别适合放在 PowerShell 脚本里。
10. **端口 3000 被占用不代表窗口仍开着**：关闭 bat 窗口不一定结束 Python 子进程，需要按项目路径识别并停止服务。
11. **第三方中转返回的模型名不可靠**：请求 `nano-banana-2`，日志可能记录为 `nano-banana-pro`；不能只根据响应名决定前端参数。
12. **网络断开不等于上游没收到任务**：自动重试可能重复扣费，因此需要区分可安全重试与不可确定状态。
13. **大范围“修复bug”提交会留下测试脱节**：`a581fbb` 删除了实现却保留测试，说明以后大规模合并必须跑完整 Python + Node 测试。
14. **`hidden:true` 不是普通配置项**：它是 RunningHub 删除墓碑。备份若不先过滤，会把已删除项目展示为可导入内容，导入后又被界面隐藏，造成“明明导入成功却什么都没有”的错觉。

## 十、下一步建议顺序

1. **先做发布卫生**：隔离 `.env` 和用户数据，处理 HTML 缓存号，清理调试文件。
2. **做一次干净安装与更新演练**：ZIP 下载、安装依赖、启动、局域网访问、程序内更新、备份导入各走一遍。
3. **实测模型图片参数策略**：分别验证 GPT Image 自适应、Banana 固定比例和供应商特殊模型 ID 的手动覆盖。
4. **补充图片接口模式说明和请求诊断**：让用户无需猜测“标准 / JSON / 中转 / RS / 异步”的区别。
5. **完成回归后再发布新版本**：更新 `VERSION`、`static/update-notes.json`、缓存版本号和 GitHub 提交说明。

## 十一、交接检查表

新会话接手前先检查：

- [ ] 阅读本文件。
- [ ] 运行 `git status --short --branch`，不要覆盖用户未提交内容。
- [ ] 不读取或输出 `API/.env` 的秘密值。
- [ ] 确认当前分支仍是 `my-custom`，远端仍是 `MAOMAOCHONGNE/Infinite-Canvas`。
- [ ] 修改普通画布前检查智能画布是否有共享模块。
- [ ] 修改 API 参数前确认 RunningHub 路径不受影响。
- [ ] 运行全部 Node 测试和 Python 测试。
- [ ] 浏览器验证只使用临时节点 / 临时画布，不把测试数据写进用户正式画布。
- [ ] 未经明确要求，不提交、不推送 GitHub。

## 十二、本状态文件维护规则

每次完成一批功能后，只需要更新四处：

1. “当前仓库状态”中的版本、提交和测试结果。
2. “本次长期会话完成的工作”中新增完成项。
3. “尚未完成 / 当前卡点”中删除已解决事项并添加新问题。
4. “下一步建议顺序”中的优先级。

不要把 API Key、Token、密码、客户素材路径、完整上游响应或个人隐私写入本文件。
