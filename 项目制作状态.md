# Infinite Canvas 定制版｜项目制作状态

> 最后更新：2026-09-05
> 实际仓库：`F:\qianse-Canvas\Infinite-Canvas`
> 当前分支：`my-custom`
> 当前已发布版本：`2026.09.05-custom.3`（GitHub 与魔塔已同步）
> 用途：新会话接手、继续开发、发布前检查和风险交接。

## 当前项目总览（2026-09-05）

### 项目背景

这是一个基于 Infinite Canvas 的本地 AI 创作工作台，包含普通/智能画布、图片生成、一键主图、一键详情页、在线生图、GPT 对话、模式案例、素材管理和备份恢复。项目长期在同一工作区内迭代，代码、测试、规划记录和本机数据同时存在，因此任何清理、迁移或发布操作都必须区分“代码改动”和“真实用户数据操作”。

### 核心目标

- 让画布和生图流程易懂、可恢复，并避免断线重试造成重复提交或重复扣费。
- 让一键主图、 一键详情页和图片生成的任务、历史、媒体与清理行为边界清晰。
- 生成记录删除应立即反馈；物理图片清理由后台处理，并只进入 Windows 回收站，不永久删除。
- 保持画布、图片生成、案例、备份和普通素材之间的数据隔离；更新或发布不能覆盖本机密钥、素材、画布和历史。
- 所有高风险行为都应有自动测试、浏览器验证、失败关闭策略和可交接的状态记录。

### 当前状态

| 范围 | 状态 | 当前规则/结论 |
|---|---|---|
| 画布删除 | 已完成 | 保持原有逻辑：删除节点不自动物理清理；画布日志和生成记录保留，存储清理仍是独立入口。 |
| 图片生成删除 | 已完成 | 保持原有图片生成清理逻辑，不纳入本次一键模块的简化规则。 |
| 一键主图删除 | 已完成 | 单图/整组记录先删并立即返回 `202`；生成图后台直接进入回收站流程；上传图只核对其它一键主图任务。 |
| 一键详情页删除 | 已完成 | 与主图规则一致，但只核对其它一键详情页任务；主图与详情页互不保护上传图。 |
| 后台清理 | 已完成 | 单线程持久化队列、回收站事务、失败重试和旧任务兼容保留；不再对每次新删除做全局媒体索引扫描。 |
| 正式媒体 | 已完成 | 支持 `assets/image-generation/media` CAS 图片和配套元数据 JSON；图片与 JSON 成对处理。 |
| 服务运行 | 已完成 | 更新后的服务已启动；队列为 `0`，主图、详情页、图片生成和队列接口健康检查均为 HTTP `200`。 |
| 发布状态 | 已发布 | GitHub `my-custom` 与魔塔 `master` 已同步；本机配置、素材和审计文件仍保留在工作区。 |

## 本次会话记录（2026-09-05）

### 做了什么

1. 按最终确认的简化规则完成了一键主图/详情页删除后端调整：
   - 删除生成图不再等待全局引用扫描，交给后台回收站队列；
   - 删除整组时直接处理该组生成图；
   - 上传图只在同一业务模块内做引用保护；
   - 主图不检查详情页，详情页不检查主图；
   - 画布和图片生成删除逻辑没有改动。
2. 保留必要的安全边界：路径检查、文件大小/修改时间复核、回收站事务、失败回滚和自动重试；这不是全库扫描，也不读取整张图片计算哈希。
3. 兼容旧版清理任务：没有 `same-module-inputs-v1` 策略的旧任务继续走旧安全流程，避免服务重启后改变旧任务语义。
4. 重启更新后的服务并做运行态检查：
   - `/api/queue_status`：HTTP `200`，清理队列 `0`；
   - `/api/main-image-tasks`：HTTP `200`；
   - `/api/detail-page-tasks`：HTTP `200`；
   - `/api/image-generation-tasks`：HTTP `200`。
5. 复跑前端专项测试：`85/85` 通过，覆盖主图/详情页分组删除、单屏删除、`202` 响应、最后一屏、清理提示和相关页面行为。
6. 通过 Python 编译检查、主图/详情页 JavaScript 语法检查和 `git diff --check`（仅有既有 CRLF 提示，无新增差异错误）。

### 本次没有做什么

- 没有点击真实用户的主图或详情页删除按钮。
- 没有把真实用户图片移动到 Windows 回收站。
- 没有调用付费图片生成接口。
- 没有读取或修改 `API/.env`、`data/asset_library.json`。
- 没有提交 Git、推送、发布或覆盖工作区既有改动。

## 还剩什么

### 必须做的验收

- 使用一组明确可丢弃的主图测试记录，验证单图删除：记录立即消失，生成图后台进入回收站。
- 使用一组明确可丢弃的详情页测试记录，验证同样行为。
- 在同一模块内复用上传图，删除其中一个任务，确认仍被另一任务使用的上传图保留。
- 验证主图与详情页之间的上传图不会互相保护，符合当前已确认的模块隔离规则。
- 验证最后一张屏删除时空分组消失；无图片时返回 `cleanup_status=skipped`。
- 查看清理失败/重试提示和服务重启后的队列恢复。

### 发布前工作

- 先审阅工作区全部未提交修改，确认没有把本机配置或真实数据误纳入发布包。
- 再决定是否创建提交、构建安装包和发布版本；当前用户尚未授权这些动作。
- 若要使用 Python 测试套件，需要补齐 `pytest` 环境，或继续使用项目现有的 `unittest` 入口；本机内置 Python 当前没有 `pytest` 模块。

## 当前卡点与风险

| 项目 | 影响 | 处理建议 |
|---|---|---|
| Python 运行时没有 `pytest` | 不能用 `pytest` 命令复跑测试 | 使用项目现有 `unittest` 测试入口；若发布前需要 pytest，再单独准备开发环境。 |
| 工作区有大量既有未提交改动 | 不能用整体回退或全量暂存来整理本次改动 | 只按需求相关文件审阅和提交，保留其它用户改动。 |
| 尚未做真实回收站验收 | 代码测试通过不等于 Windows 回收站行为已在本机完整走通 | 只使用可丢弃测试记录，不碰用户正式素材。 |
| 旧清理任务可能使用旧策略 | 旧任务与新删除语义不同 | 旧任务继续走旧安全路径，先审计再确认，不自动放行。 |
| 媒体目录同时存在 CAS 和旧 `online_*` 路径 | 手工按文件名删除容易漏掉 JSON 或误删共享输入 | 必须通过业务删除流程和回收站队列处理，不手动删除。 |

## 踩过的坑与已固化的规则

- **不能把一键删除复用成全局存储清理。** 这会在图片数量增长后触发数秒级甚至更久的全库扫描，造成 CPU/磁盘峰值；现在一键模块只处理本模块可确定的候选。
- **不能用“全局所有历史记录”保护一键上传图。** 主图和详情页是分开的业务模块，当前规则只核对本模块上传图的引用。
- **不能把生成图和上传图混成一个候选集合。** 生成图按删除范围直接处理；上传图必须在同模块内确认是否仍被其它任务使用。
- **不能只处理 PNG/JPG 而漏掉 CAS 元数据 JSON。** 正式媒体图片与元数据必须成对进入回收站；失败时原位保留。
- **不能让后台任务在记录事务完成前开始扫描。** 清理任务需要先处于 `preparing`，记录、历史和内存状态提交成功后再激活为 `queued`，避免竞态导致图片被误判为仍在使用。
- **不能把 HTTP `200` 当成删除流程唯一成功标准。** 一键删除的新契约是记录删除立即返回 `202`，前端不能等待整页刷新或下一次历史请求。
- **不能把测试环境缺少 pytest 当成业务失败。** 项目当前可用的 Node `--test` 与 Python `unittest` 仍需作为主要回归入口。

## 下一步建议

1. 先不要处理旧的真实失败清理任务，也不要手工删除 `assets/output` 或 `assets/image-generation/media` 文件。
2. 准备一组可丢弃的主图和详情页测试记录，按“单图 → 最后一屏 → 同模块共享上传图”顺序做浏览器验收。
3. 记录每次删除的 HTTP 状态、页面消失耗时、清理任务状态、回收站条目和共享文件状态。
4. 若验收全部通过，再单独评估是否需要把旧失败任务迁移、重新审计或纳入发布版本。
5. 发布前再审阅本文件和 `progress.md`，把真实验收结果、剩余异常和发布决策补回本文件。

## 一、当前结论

- 项目是基于 `hero8152/Infinite-Canvas` 深度定制的本地 AI 无限画布，不是重新创建的空项目。
- 最近一次正式发布是 2026-08-22：GitHub 提交 `b50ea71`，魔塔镜像提交 `63159e8`，两边发布树一致。
- 2026-08-22 之后已经继续完成一键主图、图片生成页面、详情页异步恢复与备份、普通画布大规模节点性能、存储清理等功能，但这些后续修改仍在本机工作区，尚未再次发布。
- 当前工作区很脏，混合了程序代码、测试、规划记录、本机配置和用户数据。不能使用 `git reset --hard`、整体回退或全量暂存。
- 最新已记录完整回归为 Python `446/446`（4 项按环境跳过）和 Node `615/615`；本次简化删除逻辑相关的存储清理专项为 `58/58`，前端专项为 `85/85`。
- 一键主图/详情页单图与分组删除、CAS/JSON 成对处理、同模块共享上传图保护和回收站事务均有专项覆盖；Python/JavaScript 语法检查通过。
- 图片生成页已在用户描述标题栏增加“提示词”入口：复用提示词库接口和无限画布缩略图卡片，正常模式双击替换用户描述，历史只读模式双击复制正向提示词；图片生成专项测试 `71/71` 通过。
- 3000 端口服务已在本次会话重新启动并完成只读健康检查；没有提交新的真实生成任务、没有调用付费接口、没有执行真实图片清理。

## 二、项目背景

本项目保留 Infinite Canvas 原有画布、节点和多平台生成能力，在此基础上持续解决以下实际问题：

- 普通用户难以理解复杂节点、接口协议和任务状态。
- 多个图片任务并发时，网络断线可能造成“上游成功并扣费，但本机没有收到结果”。
- 普通画布在数百图片、节点和连线下缩放、拖动和框选容易卡顿。
- 一键详情页、主图和通用图片处理需要独立历史、永久编号、断线恢复与备份迁移。
- 软件需要从 GitHub 或魔塔更新，同时绝不能覆盖本机密钥、素材、画布和任务历史。

定制版与原作者仓库保持分离：

| 远端 | 地址 / 分支 | 作用 |
|---|---|---|
| `origin` | `MAOMAOCHONGNE/Infinite-Canvas` / `my-custom` | 定制版 GitHub 开发与更新源 |
| `modelscope` | `qisese70/Infinite-Canvas` / `master` | 国内魔塔镜像与备用更新源 |
| `upstream` | `hero8152/Infinite-Canvas` / `main` | 查看原作者更新，不能直接覆盖定制版 |

## 三、核心目标

1. **容易理解**：界面和状态尽量使用普通用户能看懂的中文，不暴露内部协议字段。
2. **避免重复扣费**：异步任务拿到任务号后只查询，不因断线盲目重新提交。
3. **任务可恢复**：切换页面、刷新或服务重启后，能够恢复有任务号的后台查询。
4. **大画布流畅**：数百节点、图片和连线下仍能缩放、拖动、框选和定位。
5. **视觉保持稳定**：性能简化只能减少细节，不能移动节点、改变数量或创造不存在的内容。
6. **历史可迁移**：画布、详情页、主图、图片生成模式和历史能够安全导出、导入。
7. **数据与程序分离**：更新和发布不能覆盖 `data/`、`assets/`、`output/` 或本机密钥。
8. **多平台兼容**：兼容 OpenAI、OpenAI 异步、Gemini、方舟、RunningHub、ModelScope、即梦 CLI 和局域网 ComfyUI 等路径。
9. **可持续维护**：功能必须有自动测试、浏览器验证、状态记录和明确的发布边界。

## 四、技术结构与数据边界

### 4.1 技术形态

- 后端：Python、FastAPI、Pydantic、HTTPX、Pillow。
- 前端：原生 HTML、CSS、JavaScript，通过 `static/index.html` 挂载各独立页面。
- 测试：Node 内置 test runner 与 Python `unittest`。
- 任务与用户数据：本机 JSON、媒体文件和内容寻址素材目录。

### 4.2 主要模块

| 路径 | 作用 | 注意事项 |
|---|---|---|
| `main.py` | 后端接口、任务生命周期、模型调用、更新与备份 | 核心文件，任何修改都需要完整 Python 回归 |
| `static/js/canvas.js` | 普通画布控制器 | 当前改动最多，需防止破坏节点运行和保存格式 |
| `static/js/classic-canvas-performance.js` | 普通画布性能辅助逻辑 | 负责虚拟化、简化/极远景和批处理策略 |
| `static/smart-canvas.html`、`static/js/smart-canvas.js` | 智能画布 | 与普通画布相互独立，不能错误共用状态 |
| `detail_page_v4.py`、`static/detail-page.html` | 一键详情页 | 独立任务、永久编号、异步恢复和历史 |
| `main_image_v4.py`、`static/main-image.html` | 一键主图 | 连续/创意主图、规划、候选与独立历史 |
| `image_generation_modes.py` | 图片生成模式与最终提示词组合 | 原始预设、用户描述和图片职责的组合边界 |
| `image_generation_store.py` | 图片生成模式、版本、草稿、示范和任务存储 | 管理员与普通用户序列化必须隔离 |
| `image_generation_media.py` | 图片生成内容寻址媒体库 | 负责哈希去重、缩略图和回收站 |
| `image_generation_cleanup.py`、`storage_cleanup.py` | 图片生成与全局存储清理事务 | 分阶段校验；回收站不可用时失败关闭，不永久删除 |
| `static/asset-manager.html`、`static/js/asset-manager.js` | 素材管理、偏好设置与存储清理入口 | 必须与清理后端和样式成套发布 |
| `backup_transfer.py` | 备份格式、清洗、导入迁移和安全校验 | 密钥过滤和事务回滚不能削弱 |
| `tests/` | Node 与 Python 回归测试 | 当前有 75 个 Node 测试文件、33 个 Python 测试文件 |

### 4.3 必须保护的数据

| 路径 | 内容 | 规则 |
|---|---|---|
| `API/.env` | API Key、Token 等本机秘密 | 不读取明文、不写入文档、不提交、不导出 |
| `data/` | 画布、任务、配置、模式、历史与用户索引 | 不随程序更新覆盖，不整体清理 |
| `assets/` | 上传图、任务媒体、缩略图等 | 删除前必须确认引用关系 |
| `output/` | 生成结果与测试截图 | 不作为普通源代码整体发布 |
| `.planning/` | 开发计划、发现和进度记录 | 默认属于本机维护记录，发布时单独决定 |

## 五、当前发布与工作区状态

### 5.1 已发布状态

| 项目 | 当前值 |
|---|---|
| 当前分支 | `my-custom` |
| 当前 HEAD | `b50ea71` |
| GitHub | `origin/my-custom` 已发布到 `b50ea71` |
| 魔塔 | `master` 镜像为 `63159e8`，树内容与 GitHub 一致 |
| `VERSION` | `2026.09.05-custom.3` |
| `main.py::APP_VERSION` | `2026.09.05-custom.3` |
| 更新说明 | `static/update-notes.json` 同为 `2026.09.05-custom.3` |

版本文件目前一致，旧状态文件中记录的版本常量错位已经解决。

### 5.2 尚未发布的本机改动

当前本机至少包含以下后续功能的源代码与测试：

- 一键详情页永久编号、Comfly 显式异步恢复与历史备份。
- 独立一键主图页面、任务、历史和备份。
- 普通画布性能模式、极远景渲染和视图工具条。
- 独立图片生成页面、官方模式备份分发、管理功能、媒体存储和备份。
- 图片生成预览与完整比例支持。

同时存在本机敏感配置和用户数据改动，例如 `API/.env`、`data/asset_library.json`。后续发布必须使用精确文件列表暂存，不能执行 `git add .`。

## 六、功能当前状态

### 6.1 普通画布

状态：**可用，近期重点完成大画布性能优化；后续修改尚未发布。**

- 支持 IMAGE、OUTPUT、API、LLM、RunningHub、循环、视频、音频、区域框、便签等节点及连接流程。
- 支持多选、Ctrl 框选、节点拖动、区域框成员移动、复制、撤销/重做和流程运行。
- 性能模式默认开启，可在左下角按画布手动关闭；开关只保存在本机，不写入画布文件。
- `≤40%` 进入简化模式，`≥50%` 恢复完整版；简化图片使用 128px 预览。
- `≤15%` 进入极远景，`≥22%` 恢复普通简化模式，使用回差避免临界点反复跳变。
- 极远景直接创建轻量节点，不先创建完整表单；节点位置、尺寸、端口、节点类型特征和 OUTPUT 图片格子数量保持不变。
- 极远景 OUTPUT 使用 Canvas 绘制缩略格，普通连线使用缓存 Canvas；放大或操作节点后恢复交互式界面与 SVG 连线。
- 选中、拖动、编辑、播放和运行中的节点始终保留完整节点，不会因缩放而停止任务。
- 滚轮通过 `requestAnimationFrame` 合并，每帧最多更新一次；停止约 200ms 后才保存视口。
- 小地图下方已有小地图开关、网格吸附、适合全部和缩放比例工具。
- 500 节点、974 连线合成压力样本已经完成浏览器验证，节点和连线数据未变化。

### 6.2 智能画布

状态：**保持既有可用，本轮没有套用普通画布的极远景实现。**

- 智能画布仍有自己的渲染、循环、LLM、节点运行和历史逻辑。
- 普通画布的性能修改不得直接复制到智能画布，除非确认两边数据结构和生命周期一致。
- 用户实测同等大量图片下智能画布比旧普通画布更流畅，普通画布近期优化以此为体验参照。

### 6.3 一键详情页

状态：**核心功能完成；最近增强尚未再次发布。**

- 分组使用后端原子分配的永久 `group_no`，删除后不回收编号；旧任务按创建顺序迁移。
- Comfly 使用显式异步：编辑、文生图提交后取得 `task_id`，查询 `/v1/images/tasks/{task_id}`。
- 候选先建立并保存，拿到任务号后先持久化，再开始轮询。
- 已有任务号的断线会进入“恢复查询”，只继续 GET 查询，不重新提交图片生成。
- 没有任务号的断线标记“结果未知”，不自动重试，避免重复扣费。
- 自动查询最长 30 分钟；超时且有任务号时显示“回补”，回补仍只查询。
- 服务重启后恢复有任务号的未结束查询。
- 历史接入全局备份 v2，可逐组选择、迁移输入图、参考图、候选和最终图。
- 导入生成新任务 ID、提交 ID 和本机永久编号，活动任务导入后标记为“导入时已中断”。
- 当前最多 200 组，超限整次阻止，不自动删除旧记录。
- 删除分组会清理所属任务/历史记录和未被引用的生成、上传媒体；共享输入按其它任务与全局引用保留，清理交接失败整体回滚到原状态。

明确限制：旧任务若当时没有保存 `task_id`，无法自动找到上游历史结果。

### 6.4 一键主图

状态：**独立页面和后端已实现，尚未正式发布。**

- 与一键详情页完全隔离，拥有独立任务目录、永久编号、200 组历史和备份分类。
- 支持连续主图、创意主图，默认 1:1、2K、4 张、无模特。
- 产品图与参考图合计最多 6 张，一组可生成 1–12 张，页面最多同时执行 10 个图片请求。
- 可选“智能识别”；不会覆盖用户已填写内容。
- 用户界面统一为“产品名称、产品特点、用户指令”，降低“产品事实/功能卖点”的理解成本。
- 每条提示词由程序规范唯一比例/类型前缀，移除内部方括号标签，逐条追加用户产品特点和产品一致性固定句。
- 连续主图每屏独立完整，不依赖上一屏；创意主图使用独立构图，不伪装成连续叙事。
- Comfly 任务号、断线恢复、回补和重复提交保护沿用已验证异步能力。
- 全局备份 v3 可迁移主图任务、输入、提示词、候选与最终图片。
- 新生成的通用历史记录会写入主图来源标记；删除主图分组会删除该组历史行并清理未被引用的生成/上传媒体，单张主图删除仍只处理该屏的历史输入引用。
- 旧版本没有来源标记的主图历史按“主图提示词 + 产品角色引用 + 不属于现存主图组输入集合”做严格兼容识别；无法安全判断时继续保护，不猜测删除。

### 6.5 图片生成

状态：**独立页面已实现，本次发布增加空公版种子与官方备份分发。**

- 公版只内置合法空种子；官方模式、编号、提示词和案例图由维护者通过备份包定向导入，已有本地模式不会被启动清空。
- 支持搜索、分类、收藏、最近使用、模式工作台、每模式草稿、当前模式历史和全部历史。
- 普通用户只能使用模式，不显示内部预设提示词；本机管理模式可以查看、编辑、测试、启用、回退、复制、停用、恢复和删除模式。
- 管理入口只用于防止普通用户误看误改，不是面向不可信本机用户的强安全权限系统；管理口令不在本文件记录。
- 管理员可从成功任务设置固定“处理前 / 处理后”示范和示范用户描述。
- 模式、提示词版本、草稿、示范和任务均保存在本机；原始预设不被覆盖。
- 上传和结果媒体按 SHA-256 去重，使用独立内容寻址目录；未引用素材经过完整引用扫描和二次确认后移入 Windows 回收站。
- “数据管理/存储清理”分为未引用生成图和未引用临时上传图；已取消 24 小时保护，但不提供永久删除降级。案例、模式配置、草稿、收藏、最近使用、接口配置和被其他数据引用的图片始终受保护。
- 结果图可直接点击预览，预览使用主题浅色舞台和固定 `contain` 显示，不再出现黑底或大图撑出弹窗。
- 预览支持“默认对比、铺满对比、左右对比”；没有处理前图片时自动隐藏对比按钮。
- 比例支持 14 个固定项：`1:1`、`1:4`、`1:8`、`2:3`、`3:2`、`3:4`、`4:1`、`4:3`、`4:5`、`5:4`、`8:1`、`9:16`、`16:9`、`21:9`。
- 额外支持拉伸适配、自适应和自定义比例；自定义宽高限定为 1–999 正整数。
- 拉伸适配由后端根据第一张已持久化输入图重新计算，客户端不能伪造拉伸比例或像素尺寸。
- 自适应强制不指定 `aspect_ratio` 且使用 `size=auto`。
- 全局备份 v4 可迁移模式、提示词版本、示范、任务和媒体，导入继续兼容 v1–v3。
- 官方模式使用稳定 `source_id` 和内容版本；导入支持原地更新、新模式追加、重复幂等、编号冲突回滚、低版本拒绝和已移除模式停用。

### 6.6 备份与更新

状态：**已形成统一系统；新页面对应备份扩展尚未发布。**

备份版本：

| 版本 | 内容 |
|---|---|
| v1 | 传统项目、画布与配置 |
| v2 | 增加一键详情页历史 |
| v3 | 增加一键主图历史 |
| v4 | 增加图片生成模式、示范与历史 |

- 新版导入兼容 v1–v4。
- 媒体按 SHA-256 去重，导入采用新 ID、新提交 ID 和新本机永久编号。
- 运行中的跨电脑任务统一导入为“导入时已中断”，不会继续另一台机器的上游请求。
- API Key、Token、管理口令、管理令牌、Base64 和临时鉴权地址不进入备份。
- 公网图片下载失败不会阻断整个备份，会进入缺失媒体清单并在历史中标记。
- 更新器只覆盖程序白名单，保护 `.env`、`data/`、`assets/` 和 `output/`。
- GitHub 与魔塔使用不同提交历史；魔塔发布必须在其当前历史上创建相同文件树的镜像提交，不能强推覆盖。

## 七、本次长期会话完成的工作

本节记录 2026-08-22 至 2026-08-26 这轮连续开发。

### 7.1 一键详情页

- 查明 `Server disconnected without sending a response` 代表 HTTP 响应未送回，不代表上游一定失败。
- 根据 Comfly 文档把详情页改为显式异步提交和任务查询。
- 完成永久分组编号、任务号先持久化、断线续查、服务重启恢复和只查询式回补。
- 把详情页完整历史和媒体接入全局备份，加入容量校验、媒体去重和事务回滚。

### 7.2 一键主图

- 分析旧“心宝 AI 一键主图 V4”的连续/创意提示词特点。
- 新增独立一键主图页面、后端任务、历史、智能识别、候选操作、异步恢复和备份。
- 根据用户实测把界面字段统一为“产品特点”。
- 修复提示词重复前缀、内部 `【】` 标签、产品特点遗漏和固定一致性句缺失。
- 兼容旧产品信息标题和中英文提示词校验。

### 7.3 普通画布流畅度

- 诊断普通画布与智能画布在大量图片、节点和连线下的性能差异。
- 优化节点拖动、Ctrl 框选、区域框选择及大量图片节点虚拟化。
- 将性能模式改为普通画布默认开启，并允许按画布关闭。
- 反复修正简化节点的视觉突变，确保节点位置、尺寸、数量和类型特征不改变。
- 增加滚轮帧合并、停止后保存、极远景轻量节点、OUTPUT Canvas 和连线缓存层。
- 增加小地图、网格吸附、适合全部和缩放比例视图工具条。
- 浏览器逐类对比 16 类节点，并完成 500 节点 / 974 连线压力验证。

### 7.4 独立图片生成页面

- 根据公开模式配置设计并实现独立“图片生成”页面和 51 个本机模式。
- 增加本机管理模式、提示词版本、示范图、示范用户描述、草稿、历史、素材存储和回收站。
- 增加图片生成任务的幂等、防重复、异步恢复和全局备份 v4。
- 修复结果预览显示不全和黑色背景。
- 增加默认对比、铺满对比和左右对比。
- 补齐普通画布的完整比例、拉伸适配、自适应和自定义比例。

### 7.5 发布

- 2026-08-22 将当时已完成的详情页与画布增强发布到 GitHub 和魔塔。
- 发布过程使用精确暂存和密钥扫描，没有上传 `.env`、用户素材、任务历史或规划文件。
- 2026-08-22 之后的本机修改未再次发布，也没有提升版本号。

### 7.6 发布候选演练

- 新增 `发布候选文件清单.md`，把 60 个待发布程序、测试、内置数据和维护文件与本机数据明确分开；本轮补齐存储清理运行时和资产管理页面资源。
- 新增 `tools/build-release-candidate.ps1`，以已发布 `HEAD` 为安全基线，只覆盖审核白名单，并强制在系统临时目录构造候选树。
- 候选构造会验证 `API/.env` 和 `data/asset_library.json` 保持 `HEAD` 基线，拒绝规划目录、一次性发布助手、历史、日志和调试残留进入候选。
- 删除无用途的浏览器报错残留 `el.click()`；一次性 `_modelscope_sync_once.py` 保留在本机但明确排除发布。
- 修复两组只在本机配置齐全时才通过的测试：详情页异步测试改为显式模拟测试 Key，图片生成忽略规则测试改为使用临时 Git 仓库。
- 调整主导航为“本地功能、在线生图、GPT 对话、一键主图、一键详情页、图片生成、无限画布”，并加入 DOM 顺序回归测试。
- 紧凑化图片生成模式卡片：模式大厅铺满可用宽度并自适应列数；编号和名称单行省略；隐藏静态用途、分类和上传数量，仅保留活动任务进度。
- 上一轮干净候选共 370 个文件、约 55.3 MB；本轮更新白名单后隔离候选共 462 个文件、约 296.9 MB，普通 HTTP 加载验证一键主图、图片生成、一键详情页、无限画布列表和素材管理入口均为 200，未调用付费接口。
- 备份 v4 临时数据演练覆盖导出检查、CAS 媒体恢复、重复导入独立性、失败回滚和 200 条容量预检，结果 4/4 通过。

### 7.7 2026-09-03 一键主图与存储清理引用修复

- 复现用户问题：删除一组“一键主图”后，进入“存储清理 → 清理未引用临时上传图”仍提示“当前没有可清理的未引用临时上传图”。
- 根因：一键主图调用通用生图流程写入全局 `history.json`；旧版删除分组只删主图任务文件，没有解除历史中的上传图引用。
- 新任务在首次写入历史时增加 `source_type`、来源任务和屏幕信息；删除整个分组会彻底移除该组拥有的历史行，删除单屏仍只处理该屏自身记录。
- 对旧数据增加保守兼容路径：严格识别主图提示词、输入图角色和现存主图组集合；若无法确定归属，继续保护，不执行猜测性清理。
- 生成图和上传图不会阻塞分组删除响应；持久化后台清理会扫描任务、历史、草稿、案例、收藏和运行时引用，确认无引用后移入 Windows 回收站。
- 真实本机此前只读检查曾识别 3 个候选；本轮使用正确的 `kind=temporary-upload` 重新预览返回 `has_targets=false`，未使用确认令牌、未移动任何媒体。
- 修复时曾因缩进错误让主图请求暂时绕过 10 路信号量，峰值达到 12；已修正并通过并发回归。

## 八、验证记录

### 8.1 最新完整回归

| 检查 | 结果 |
|---|---|
| 上一次隔离候选 Node 回归 | `561/561` 通过 |
| 上一次隔离候选 Python 回归 | `330/330` 通过 |
| 当前会话 Python 完整回归 | `379/379` 通过，跳过 4 项 |
| 当前会话 Node 完整回归 | 排除本机 `static/runninghub/api_providers.json` 非法 JSON 夹具后 `601/601` 通过；全量仅该夹具对应测试失败 |
| 更新后隔离候选 Node 回归 | `598/598` 通过 |
| 更新后隔离候选 Python 回归 | `369/369` 通过，跳过 4 项 |
| 当前主图/存储清理专项 | `21/21` 通过 |
| 素材管理 Node 专项 | `4/4` 通过 |
| JavaScript 语法 | 通过 |
| Python 编译 | 通过 |
| `git diff --check` | 用户维护的 `static/runninghub/api_providers.json` 有两处尾随空格；排除该路径的范围检查通过，未修改用户文件 |

### 8.2 浏览器验收

- 当前本机服务访问 `http://127.0.0.1:3000` 正常。
- 图片生成结果图直接点击可打开预览。
- 默认、铺满、左右三种对比在桌面和 `390×844` 窄屏均完整显示。
- 预览非黑底，关闭、下载和模式按钮没有超出窗口。
- 14 个固定比例和三个特殊比例均出现在界面。
- 现有输入图实测“拉伸适配”识别为 `4:3`；自定义比例能恢复为 `16:9` 输入。
- 浏览器控制台 0 错误。
- 隔离候选服务 `127.0.0.1:3017` 已在普通 Chrome 加载一键主图、图片生成 51 模式、一键详情页和无限画布列表，四个入口均无控制台错误。

所有自动测试和浏览器验收都使用模拟响应或现有成功历史，没有提交真实付费生成请求。

### 8.3 当前会话服务验证

- `python -m py_compile main.py tests/test_storage_cleanup.py tests/test_detail_page_async_recovery.py tests/test_detail_page_tasks.py tests/test_main_image_tasks.py` 通过。
- 排除用户维护的 `static/runninghub/api_providers.json` 后 `git diff --check` 通过；全量检查仅报告该文件两处尾随空格，未修改。
- 3000 端口已精确重启为 PID `70576`；两个任务 API 与主图/详情页静态资源均返回 HTTP 200。
- `POST /api/storage-cleanup/preview`（`kind=temporary-upload`）在 3000 服务和隔离候选 3018 均返回成功、`has_targets=false`；没有执行确认接口，也没有移动或删除文件。

## 九、尚未完成

### P0：下一次发布前必须完成

1. **已完成｜整理发布边界**：`发布候选文件清单.md` 已区分程序、测试、文档、本机配置和用户数据；构造脚本白名单现覆盖 60 条候选路径，新增存储清理运行时与资产管理资源。
2. **已完成｜排除敏感/私人内容**：候选中的 `API/.env` 和 `data/asset_library.json` 严格保持 `HEAD` 基线，本机任务、素材、输出、日志和规划目录均未进入；敏感模式扫描 0 命中。
3. **已完成｜处理调试残留**：已删除 `el.click()`；一次性 ModelScope 助手、内部设计记录和规划目录均已明确排除。
4. **已完成｜全新目录演练**：更新后的隔离候选已完成启动、五个主要页面 HTTP 加载和 Node/Python 无付费回归；浏览器视觉复核仍待正式发布前再做一次。
5. **已完成｜真实备份演练**：临时数据的 v4 导出、检查、重复导入、媒体恢复、容量拦截和失败回滚均通过。
6. **待正式发布｜更新发布身份**：同步提升 `VERSION`、`APP_VERSION`、`static/update-notes.json` 和静态缓存参数。
7. **候选已完成、暂存后重做｜完整测试和密钥扫描**：更新候选 Node `598/598`、Python `369/369` 通过；正式精确暂存后仍需对最终树再运行一次。
8. **待发布前确认｜存储清理回归**：当前预览无候选，未执行真实清理；发布前仍需用共享输入图、案例和草稿保护样本做只读复核。

### P1：建议继续验证

- 在用户明确授权费用后，为详情页、主图和图片生成分别做一次低成本 Comfly 真实异步任务，核对后台 `task_id`、查询状态和回补结果。
- 用更多真实产品测试一键主图提示词质量，重点检查产品一致性、未确认参数、参考图品牌隔离和中文画面文字。
- 为常用图片生成模式逐步制作固定“处理前 / 处理后”示范；当前没有示范的模式不影响生成。
- 在用户实际 500 图画布上继续观察极端全览、超多 OUTPUT 图片和超多连线的显存/内存占用。
- 完成一次服务重启后的详情页、主图、图片生成异步恢复验证。
- 用用户刚删除的一键主图数据打开“存储清理 → 清理未引用临时上传图”，先查看候选列表，再由用户明确确认后再移入回收站。

### P2：后续可选增强

- 把普通画布小地图标记改为单 Canvas 绘制；当前小地图关闭时已跳过更新，此项不是本轮必要条件。
- 为图片生成管理模式增加真正的多用户认证；当前仅防本机普通用户误操作。
- 增加可审核的模式预设包发布机制；当前模式管理只在本机生效。
- 根据后续供应商文档补充上游取消接口；目前无取消协议的平台只能停止本机查询。

## 十、当前卡点

1. **工作区修改范围很大**：多个功能连续开发但尚未提交，发布时最容易漏文件或误带用户数据。
2. **真实付费链路未做自动化实测**：自动测试不能证明每个中转站当前仍完全遵循文档，只能证明本机请求和恢复逻辑正确。
3. **旧断线任务缺少任务号**：历史记录中没有 `task_id` 的任务无法自动追查上游结果。
4. **上游取消能力不统一**：没有官方取消接口时，本机取消不能保证停止已经提交的上游任务。
5. **极端全览仍受浏览器上限影响**：性能已经显著改善，但数千图片格子、超长画布和低显存设备仍可能触发浏览器绘制瓶颈。
6. **本机管理模式不是安全系统**：能直接读取源码或本机数据的用户仍可能绕过页面入口。
7. **旧主图历史没有来源字段**：不能仅靠共享上传图或图片名称删除；当前只用主图提示词、输入角色和现存主图集合做保守识别，无法安全判断的记录继续保留。
8. **全局历史同时服务多个功能**：删除主图分组不能直接删除整条 `history.json`，否则会误伤普通生图或共享结果；当前修复只解除属于该组的输入引用。
9. **存储清理候选不等于立即删除**：预检查只生成一次性确认，确认前会重新扫描；回收站失败时整批回滚，不退化为永久删除。
10. **本次修复曾短暂绕过并发信号量**：历史标记代码插入时缩进错误导致 12 路并发，已恢复到 10 路范围并加入回归覆盖。

## 十一、已经踩过的坑

1. **真实项目路径不是环境默认路径**：正确目录是 `F:\qianse-Canvas\Infinite-Canvas`，`F:\Infinite-Canvas` 在本机不存在。
2. **静态缓存号遗漏会让修改看起来无效**：改完 JS/CSS 后必须同步更新对应 HTML 或外层 iframe 缓存参数。
3. **HTTP 断线不等于上游失败**：连接断开时自动重新 POST 可能重复扣费；有任务号只能查询，没有任务号只能标记未知。
4. **任务号必须先落盘再轮询**：否则服务或连接在首次查询前中断，恢复机制仍然拿不到目标。
5. **前端比例不能被信任**：拉伸适配、像素尺寸和自定义比例必须由后端重新校验和计算。
6. **简化节点不能改变视觉身份**：远景只能隐藏细节，不能移动控件、增加图片格或把多个对象合成一个对象。
7. **只换成低清图不能解决所有卡顿**：全览时真正的瓶颈还包括 DOM、表单、阴影、OUTPUT 图片格和连线重绘。
8. **运行中节点不能虚拟卸载**：缩放优化必须保留运行、编辑、播放、选中和拖动中的完整节点。
9. **模型输出格式不能完全依赖提示词**：主图提示词需要程序做确定性规范，消除重复前缀、内部标签和产品特点遗漏。
10. **浏览器图标库版本有限**：使用当前 Lucide 不存在的图标会产生控制台警告；新增图标必须先确认已打包。
11. **备份版本必须按内容升级**：详情页、主图、图片生成分别引入 v2、v3、v4，不能让旧软件静默丢失新数据。
12. **导入运行中任务不能跨电脑继续**：否则可能在另一台机器继续查询、取消或产生费用，必须统一中断化。
13. **Windows PowerShell 与 Bash 语法不同**：不要使用 heredoc；复杂管道应拆开，避免空管道解析错误。
14. **项目没有标准 npm 测试脚本**：Node 测试使用内置 `node --test`；Python 使用 `unittest discover`，不能假定存在 pytest。
15. **大段测试输出会让开发工具界面卡顿**：完整回归应使用 dot reporter 或限制输出；这不会停止 Infinite Canvas 服务或任务。
16. **不能全量暂存**：`.env`、用户数据、媒体和规划文件与业务代码混在同一工作区时，必须逐文件审核。
17. **测试不能暗中依赖本机配置或 `.git`**：本机全绿不代表 ZIP 候选全绿；模拟 provider 必须显式提供测试鉴权，Git 忽略规则测试必须自己创建临时仓库。
18. **内置验收浏览器不等于普通 Chrome**：父页面与 iframe 同时加载 Tailwind 时，内置浏览器会报告一个 MutationObserver 隔离错误；普通 Chrome 对同一候选 0 error，不能为了测试工具特性改坏产品布局。

## 十二、下一步建议顺序

1. 当前服务的存储清理预览已复核为无候选；若后续出现候选，仍需先确认归属，未经用户确认不点击回收站操作。
2. 继续冻结功能范围，按 `发布候选文件清单.md` 精确暂存程序、测试、内置数据和必要说明文件。
3. 检查暂存区，确认 `API/.env`、`data/asset_library.json`、本机媒体、历史、规划记录和一次性助手均未进入。
4. 正式提升版本号、更新说明和所有相关静态缓存参数。
5. 基于最终暂存树再运行 Node/Python、编译、差异和敏感信息检查；Node 的本机 JSON 夹具需单独修复或明确排除。
6. 重启一次隔离测试服务，验证三类异步任务的恢复状态，不提交付费请求。
7. 获得用户明确发布授权后创建本地发布提交，先快进推送 GitHub，再在魔塔现有历史上创建相同树的镜像提交，禁止强推。
8. 发布后从两个远端分别核对版本文件、树哈希和更新器可见版本。

## 十三、新会话接手检查表

- [ ] 先阅读本文件，再阅读对应 `.planning/<任务>/` 下的 `task_plan.md`、`findings.md` 和 `progress.md`。
- [ ] 固定工作目录为 `F:\qianse-Canvas\Infinite-Canvas`。
- [ ] 运行 `git status --short --branch`，保护所有未提交修改。
- [ ] 不读取、输出、备份或提交 `API/.env` 的内容。
- [ ] 不使用 `git reset --hard`、`git checkout --` 或 `git add .`。
- [ ] 修改静态资源后检查页面自身和 `static/index.html` 的缓存参数。
- [ ] 修改异步流程时检查“任务号先保存、断线不重提、重启可恢复”。
- [ ] 修改性能模式时检查节点位置、尺寸、数量、端口、连线和运行状态保持不变。
- [ ] 修改备份时同时验证 v1–v4 兼容、密钥过滤、路径/哈希校验和失败回滚。
- [ ] 发布前运行完整 Node 与 Python 回归。
- [ ] 未经用户明确要求，不提交、不推送、不上传 GitHub 或魔塔。

## 十四、常用验证命令

PowerShell 中建议使用：

```powershell
$nodeTests = @(Get-ChildItem -LiteralPath tests -Filter '*.test.cjs' -File | ForEach-Object { $_.FullName })
node --test @nodeTests

python -m unittest discover -s tests -p 'test_*.py'
python -m py_compile main.py backup_transfer.py detail_page_v4.py main_image_v4.py image_generation_store.py image_generation_modes.py image_generation_media.py

git diff --check
git status --short --branch
```

需要避免大量输出时，Node 可使用 `--test-reporter=dot`。

## 十五、维护规则

每次完成一批功能后至少更新以下内容：

1. 顶部日期、当前版本和最近发布提交。
2. “功能当前状态”中的已实现 / 已发布边界。
3. “本次长期会话完成的工作”。
4. 最新完整测试和浏览器验收结果。
5. “尚未完成”“当前卡点”和“下一步建议顺序”。

状态文件不得包含 API Key、Token、管理口令、客户素材内容、完整上游响应或个人隐私。

## 十六、2026-09-03 分组删除语义修复（当前未发布）

- 一键主图和一键详情页的“删除当前分组”现在会删除该组任务 JSON、所属全局历史记录行，以及该组生成/上传媒体中不再被其它任务、历史、画布、收藏或运行时引用的文件。
- 共享输入按完整引用扫描保护：例如 A1、A2 都使用 B1 时，删除 A1 只清理 A1 自有结果和 A1 独占上传图，B1 会因 A2 的任务记录仍在而保留。
- 生成媒体和上传媒体通过现有 Windows 回收站事务交接；回收站不可用、引用扫描失败或检测到并发新引用时，分组记录保持已删除，图片保留，清理任务记录错误并自动重试，不退化为永久删除。
- 详情页普通生成与异步回补现在写入 `source_type=detail-page`、`source_task_id`、`source_screen_no`，后续删除可精确定位所属历史行；旧记录仍使用保守的输出地址/上游任务号兼容匹配。
- 新增回归覆盖主图/详情页共享上传图保护、生成结果清理、任务与历史彻底删除、回收站失败回滚，以及详情页历史归属标记。
- 本次修改尚未升版、暂存、提交或推送；验证只使用临时测试目录，不执行真实用户素材清理。

## 十七、2026-09-03 删除提速收尾（当前未发布）

- 详情页删除确认框已补齐脚本状态、确认/取消动作和焦点恢复；主图与详情页行为一致，删除确认不再依赖原生 `window.confirm`。
- 主图和详情页后端删除路由显式拒绝运行中的分组（HTTP 409），记录删除仍保持 `202 + cleanup_job_id` 的快速返回协议。
- 两页前端会跟踪清理任务状态；清理失败提示“图片将在后台重试清理”，成功或任务记录已被服务清除后自动停止状态轮询。
- 删除期间在途轮询响应会被丢弃，避免已删除分组重新出现在缓存或历史列表；没有活动任务时立即停止任务轮询。
- 运行中保护同时在统一后端删除函数内校验，兼容调用不会绕过安全规则；专项 Python 回归 `29/29` 通过。
- 生命周期测试已隔离一键清理任务目录；本轮发现并清除的两条空清理任务为测试残留，没有对应任务记录或媒体路径，未修改用户素材。
- 删除后迟到的生图结果受统一记录锁保护，不会重新写回该分组历史；若输出文件在原清理任务收集候选后才落盘，会补建持久化清理任务。
- 最新专项回归：删除 UI `9/9`、存储清理 `21/21`、主图生命周期 `10/10`；完整 Node `601/601`（排除用户维护非法 RunningHub 夹具）、Python `379/379`（跳过 4 项）。
- 浏览器只读验证主图/详情页确认框、取消焦点和控制台状态通过；没有真实删除、生成、付费请求或发布操作。

## 十八、2026-09-03 图片生成删除统一（当前未发布）

- “图片生成”的“删除所选”、整组删除和“全部记录 → 数据管理 → 删除”现在统一为两阶段：任务/候选记录先删除并返回 HTTP `202`，媒体随后由持久化单队列后台清理。
- 清空最后一张候选时会彻底删除任务 JSON 和内存状态，不再留下“已删除”空任务，避免记录长期累计。
- “删除所选”继续保护上传输入图；整组删除和数据管理会把相关输入/输出媒体交给后台复核，只将删除后已无任何任务、草稿、案例或其它持久引用的媒体移入 Windows 回收站。
- A1/A2 共用 B1 或共用生成结果时，先删除 A1 会因 A2 的现存引用而完整保留共享媒体；最后一个引用删除后才允许清理。
- Windows 回收站失败或引用扫描异常时，记录保持已删除、图片保持原位，清理任务记录错误并指数退避重试；服务重启会把中断的 `running` 任务恢复为 `queued`。
- 持久化图片生成清理任务启动载入时会拒绝非法/重复媒体 ID 和非法截止时间，避免损坏任务进入执行队列。
- 前端收到 `202` 后立即从缓存、结果区和历史列表移除记录，不使用整页刷新；清理中、清理成功和后台重试均有明确提示，迟到轮询不会把已删除记录写回。
- 数据管理确认列表的无障碍说明已改为“记录删除与 Windows 回收站图片清单”，页面和主框架缓存号已由启动器同步更新。
- 最新验证：图片生成任务/媒体 `79/79`（4 项跳过）、存储清理 `25/25`、图片生成页面 `66/66`、完整 Python `384/384`（4 项跳过）、完整 Node `601/601`（排除用户维护的非法 RunningHub 夹具）全部通过。
- 3000 服务在确认三类生成任务活动数均为 0 后，已从 PID `52440` 安全重启为 PID `9912`；OpenAPI 三条删除路径均声明 HTTP `202`，持久化清理队列当前为空。
- 浏览器只读验收页面缓存、数据管理文案和控制台通过；没有执行真实删除、付费生成、提交、推送、发布或版本升级。

## 十九、2026-09-03 旧版 online 输出遗留修复（当前未发布）

- 图片生成清理任务现在同时保存内容寻址媒体 ID 和旧版本地路径，能处理 `/assets/output/online_*.png`，并通过统一引用扫描和 Windows 回收站事务清理。
- “删除所选”、整组删除和数据管理均补齐旧路径候选；共享输入/输出仍按完整引用保护，回收站失败继续保留文件并自动重试。
- 图片生成新历史行增加任务/候选归属；删除时只移除明确属于该候选/任务的历史行。旧历史行仅在上游任务号精确匹配时兼容处理，无法安全归属的历史继续保护。
- 数据管理新增旧版 `online_` 孤立输出扫描；未引用文件可在确认后进入回收站，不会扫描或删除其它普通输出。
- 临时目录回归覆盖旧输出单候选、共享旧输入/输出、历史归属和孤立文件清理；图片生成任务 `61/61`、存储清理及事务 `25/25` 通过。
- 用户真实文件 `assets/output/online_f86aabb36c.png` 只读核对为 `2,679,131` 字节、SHA256 `794CC9ED...9625`，仍被 `history.json` 的旧全局历史行引用，因此本轮按安全规则保留，未移动或删除。
- 本次修改尚未升版、暂存、提交或推送；未执行真实用户素材清理、付费生成或发布。
- 最新完成前验证：Python 全量 `388/388`（跳过 4 项环境相关用例），Node 全量 `602/602`，相关模块 `py_compile` 通过；3000 服务已加载更新代码并返回 HTTP 200。

## 二十、2026-09-03 正式素材库单写入与隔离区迁移（当前未发布）

- 图片生成、主图、详情页的核心生成与异步回补统一写入 `assets/image-generation/media/<哈希分片>/<SHA-256>.<ext>`，并生成配套元数据 JSON；核心模式不再新建 `assets/output/online_*`。
- RunningHub、即梦和本机 CLI 的核心路径也通过同一持久化上下文进入正式素材库；普通画布、聊天和 Midjourney 保持旧输出兼容。
- 图片生成清理任务 schema 升级为 v2，同时兼容旧 v1 任务；增加隔离区图片/JSON 清单、恢复统计和异常保留状态。
- 数据管理预览会审计 `assets/image-generation/trash`：仍被案例、草稿、任务、画布、收藏或运行时引用的有效图片先恢复 CAS，再把旧隔离图片与清单 JSON 一起移入 Windows 回收站；损坏或哈希不匹配内容原位保留并报告。
- 清理成功后仅整理确认为空的哈希分片目录；启动流程不再调用旧的隔离区永久清理函数。
- 本次没有移动用户真实文件；用户反馈的 `assets/output/online_3ee0adbe1c.png` 仍原位保留，需经过数据管理预览与确认后才会按引用结果处理。
- 最新验证：Python `393/393`（4 项预期跳过）、Node `601/601`（排除用户维护的非法 RunningHub 夹具）；3000 服务已重启为 PID `64748`，未执行真实生成、付费请求、提交或发布。

## 二十一、2026-09-03 本轮断线续接与服务收尾（当前未发布）

- 续接后重新读取项目状态、任务计划和进度记录；没有重置或覆盖工作区已有修改。
- 最后一处契约修正：主图与详情页分组删除路由显式声明 `status_code=202`，与实际 JSON 响应、图片生成删除和前端契约保持一致。
- 回归结果：Python `393/393`（4 项预期跳过）；Node `601/601`（排除用户维护的非法 RunningHub 夹具）；删除/存储专项 `31/31`；图片生成、主图页面专项 `75/75`；Python 编译、两页 JavaScript 语法检查和排除夹具后的差异检查通过。
- 服务已精确重启为 PID `73180`；`/static/main-image.html`、`/static/detail-page.html`、`/static/image-generation.html`、`/static/asset-manager.html` 和 OpenAPI 均返回 HTTP `200`。
- OpenAPI 当前明确列出主图、详情页、图片生成整组删除及候选批量删除的 `202` 响应；清理状态接口存在，`data/one_click_cleanup_jobs` 当前为 `0` 条。
- 只读检查确认图片生成、主图、详情页当前没有运行中任务；用户反馈的 `assets/output/online_3ee0adbe1c.png` 仍原位存在，未执行真实删除、生成、付费请求、提交、推送或发布。

## 二十三、2026-09-03 图片生成终态记录批量清理（当前未发布）

- “图片生成 → 全部记录”右上角新增 `清理失败/已删除（N）` 按钮。数量按全局任务摘要计算，超过 200 条时前端会继续分页读取，避免只清理第一页。
- 新增 `POST /api/image-generation-tasks/cleanup-terminal`，服务器在任务锁下再次核对状态，只允许失败和已删除终态；进行中、已完成、部分成功、待回补、已取消以及含活动候选的任务不会被此入口删除。
- 点击后先显示失败/已删除分项统计和二次确认；确认返回 `202` 后立即从列表、缓存和轮询集合移除记录，不整页刷新。任务历史先删，媒体交给持久化清理队列重新扫描引用后移入 Windows 回收站，失败自动重试。
- 新增回归覆盖失败/已删除限定、共享媒体保护、陈旧快照、活动候选保护、前端按钮/确认框/计数与移动端排版。
- 本轮验证：图片生成任务全套 `68/68`、存储/媒体相关 `64/64`（4 项预期跳过）、图片生成页面 Node `67/67`；完整 Python `397/397`（4 项预期跳过）、完整 Node `603/603`；Python 编译、JavaScript 语法和差异检查通过。
- 在前一次确认活动任务数为 `0` 后，3000 服务已安全重启并加载新代码；静态页面与 OpenAPI 均返回 HTTP `200`，新终态清理路由声明 `202`。随后只读检查发现当前服务 PID `18916`、图片生成任务 `157` 条，其中 `2` 条正在运行；不再重启或触碰这些任务。
- 本轮仅修改代码、测试、规划和状态记录；未执行真实用户清理、未调用付费生成、未读取或修改 `API/.env`，未提交、推送或发布。

## 二十二、2026-09-03 真实删除验收与旧资产定向回收（已完成，当前未发布）

- 已按用户确认真实删除一键主图失败分组 `#19`；页面约 `435ms` 内移除分组，任务 JSON 消失。
- `#19` 与 `#10/#16/#17/#18` 共用的两张输入图均保留，其他四个主图分组仍可用。
- 精确删除 `history.json` 中唯一匹配 `/assets/output/online_3ee0adbe1c.png` + 时间戳 `1788432248.47922` 的旧历史行，历史总数 `1306→1305`，未新增记录。
- 清理任务 `37dfd3f51f604a2ea58bd099674dcf48` 成功将指定旧 PNG 与 `assets/image-generation/trash` 中 88 组图片/JSON 共 177 项移入 Windows 回收站；原路径已清空，隔离区只保留空日期目录。
- 图片生成任务仍为 `155` 条，主图剩余 `4` 组，详情页 `1` 组；清理队列为 `0` 条；核心页面均 HTTP `200`，浏览器错误为 `0`。
- 本次真实维护未调用付费生成、未修改 `API/.env`、`data/asset_library.json` 或 RunningHub 配置；未提交、推送或发布。

## 二十四、2026-09-04 图片生成结果操作栏顶部固定（当前未发布）

- 按用户截图将“生成记录”标题、图片数量、全选/取消选择、下载选中、删除所选和查看案例移入工作台顶部同一行；左侧保留返回/编号/模式名，最右保留收藏。
- 结果卡片内部不再重复显示操作栏，图片区域直接从结果内容开始；桌面端随工作台固定，移动端在顶部第二行自动换行，避免滚动后按钮被遮挡。
- 更新滚动可见性回归契约；图片生成页面专项 `68/68` 通过，浏览器验证操作栏滚动后仍停留在顶部，控制台无新增错误。
- 移除“下载选中”顶部黑色悬浮提示，避免提示层被裁切成黑色残影；保留 Ctrl + 鼠标左键多选能力。
- 将数量文案简化为“生成记录 N 张”，选中数量独立显示为绿色“已选 N 张”胶囊标签；专项测试仍为 `68/68` 通过。
- 进一步按用户确认调整选择态：未选择时显示“生成记录 N 张”，选择任意图片后隐藏该组记录文字，只保留“已选 N 张”和操作按钮；记录与“全选”之间增加明显间距并移除分隔点。
- 修正选择态隐藏规则：标题容器自身的 `display:flex` 会覆盖 HTML `hidden` 默认样式，现明确加入 `[hidden] { display:none; }`；同时更新图片生成页面及外层 iframe 缓存参数，避免继续加载旧脚本/样式。
- 最新验证：图片生成页面与侧栏专项 `70/70` 通过，JavaScript 语法检查通过；浏览器验证选择 1 张后“生成记录 N 张”标题实际 `display:none` 且占位为零，取消选择后恢复显示，选择标签显示为“已选 1 张”。本次未修改后端、用户素材、历史数据或清理队列，未提交、推送或发布。

## 二十五、2026-09-03 生图结果图片自适应尺寸优化（当前未发布）

- 结果画廊新增按图片比例和可用空间计算的统一尺寸算法；单张横图、竖图以及多图行的每张图片宽高均受 `600px` 单边上限约束。
- 单张图片在结果区域居中展示；两张、三张竖图按结果区域宽度、窗口可用高度和行间距整体缩放，保持原比例、完整显示，不裁剪、不变形。
- 移除单张结果原有 `360px` 宽度硬限制；结果行使用计算后的像素宽高，桌面间距 `12px`，移动端间距 `8px`。
- 通过 `ResizeObserver` 与窗口 `resize` 监听在结果区域变化时自动重排；不改变结果分组、顺序、选择、下载、删除、预览、历史或生成逻辑。
- 图片生成页面和外层 iframe 缓存号更新为 `2026.09.03-gallery-fit.1`。
- 专项验证：图片生成页面与侧栏 Node 测试 `71/71`，JavaScript 语法检查通过；浏览器已验证单张横图、单张竖图、三图和窄视口无溢出，控制台 `error/warn=0`。
- 严格前端审计在临时源码范围内通过：`findings=[]`、`errors=0`、`warnings=0`；项目素材目录未纳入扫描，避免无关媒体拖慢审计。
- 本次未修改后端、历史数据、用户图片、生成逻辑或运行时清理队列，未执行真实生成、提交、推送或发布。

## 二十六、2026-09-03 生图结果画廊改为左对齐（当前未发布）

- 按用户反馈，结果图片不再水平居中；单张和多张结果行统一从结果区域左侧开始排列。
- 保留上一轮的 600px 单边上限、按比例缩放、完整显示、响应式重排和移动端防溢出规则。
- CSS/JS 缓存号更新为 `2026.09.03-gallery-fit.2`，专项测试断言已同步更新。
- 浏览器核验 `.2` 版本已加载，结果行实际计算为左对齐（`flex-start`）且保留 `12px` 间距；未产生控制台错误。

## 二十七、2026-09-03 生图结果三图自适应排列（当前未发布）

- 不再把 `600px` 作为固定目标尺寸；结果图片使用流动尺寸计算，设置 `700px` 仅作为安全上限，实际显示会随容器、窗口高度、比例和图片数量变化。
- 结果分组改为每行最多 3 张，三张 `16:9` 图片在桌面结果区域内会自动缩小并放在同一行；空间不足时仍保证不溢出。
- 缓存号更新为 `2026.09.03-gallery-fit.3`，并补充三图同排回归测试。
- 专项测试 `72/72`、JavaScript 语法检查通过；浏览器确认 `.3` CSS 已加载，结果行实际为左对齐并保留 `12px` 间距。

## 二十八、2026-09-04 素材管理统一存储清理（当前未发布）

- “素材管理 → 存储清理”原有两个分类按钮已合并为一个“扫描可清理文件”入口，一次扫描生成目录、旧输出目录和临时上传目录。
- 扫描结果固定显示两组核心统计：“全部文件 N 个 / 占用 X”和“可清理 N 个 / 可释放 X”，并补充生成图、临时上传图分项。
- 后端预览新增 `kind=all`，返回总文件、候选文件、受保护文件、字节数、分类 breakdown 和保护来源；确认阶段仍会再次扫描，并继续校验文件大小与修改时间。
- 弹窗会明确提示仍被“旧在线生图历史”保护的文件数量，引导用户先到在线生图归纳/管理删除对应记录，不再只显示含糊的“没有可清理内容”。
- 旧 `/api/history/delete` 已改为 HTTP `202`：历史记录立即原子删除，相关本地输出/临时输入交给 `legacy-history` 持久化后台清理任务；共享引用继续保留，最终只允许移入 Windows 回收站，不再调用 `os.remove()` 永久删除。
- 当前本机只读扫描结果：382 个文件、占用约 1.4 GB；可清理 0 个；356 个文件受旧在线生图历史保护，26 个受其它记录保护；生成图 308 个、临时上传图 74 个。
- 当前专项验证已通过：存储清理 `25/25`、存储事务 `4/4`、主图任务 `10/10`、素材管理/图片生成/删除 UI 组合 `83/83`；完整 Python `401/401`（4 项预期跳过），JavaScript/Python 语法与差异检查通过，浏览器控制台 error/warn 为 0。
- 服务启动器会按 CSS、JS、HTML 的实际修改时间自动同步缓存号；图片生成页相关测试已改为验证实际 mtime 缓存号，避免安全重启后被手工版本字符串误报，不涉及图片生成业务逻辑。
- 3000 服务已在确认无活动生成任务、清理队列为空后重启为 PID `29116`。浏览器只执行扫描预览，未确认真实回收，未移动用户文件，未调用付费生成，未提交、推送或发布。

## 二十九、2026-09-04 旧版来源未标记历史排除法清理（当前未发布）

- `type=online` 且缺少 `source_type/source_task_id` 的旧共享历史不再永久阻塞存储整理；它们被归为“旧版来源未标记弱引用”，只有在画布、案例、收藏、主图、详情页、图片生成任务、草稿、持久记录和运行中队列均无引用时才成为候选。
- 上传参考图与生成结果使用同一安全规则，不再特殊永久保护上传图；当前只读预览识别到 `240` 个旧版候选、约 `909.9 MB`，其中生成图 `220` 个、上传参考图 `20` 个，关联 `225` 条旧历史。
- 预览/确认之间使用旧历史规范化 JSON 的 SHA-256 指纹复核；历史内容变化、文件大小/修改时间变化或新出现强引用时跳过对应文件，不猜测删除。
- 确认接口改为 HTTP `202`：空结果历史行删除，有保留结果的历史只移除候选图片引用；随后创建持久化后台清理任务，完整引用扫描通过后才移入 Windows 回收站，失败保留文件并自动重试。
- 新的在线生图直接生成、RunningHub 回补/查询和画布在线生图历史开始写明确来源标记，避免未来记录继续落入来源不明集合。
- 素材管理弹窗增加旧版候选的生成图/上传参考图分项、关联历史条数和 12 个候选示例；示例在两类文件同时存在时各展示 6 个，确认按钮始终位于可视区域内。
- 回归验证：Python 完整测试 `405/405`（4 项按环境跳过），Node 完整测试 `608/608`；存储清理专项 `29/29`，素材管理专项 `7/7`，Python/JavaScript 语法及差异检查通过。
- 本地浏览器只读验收显示弹窗和确认按钮未溢出、控制台无 error/warn；未点击“确认后台清理”，未修改真实历史或图片。3000 服务在确认活动任务和清理队列均为 `0` 后重启为 PID `35512`。

## 三十、2026-09-04 固定案例旧文件审计与安全回收（当前未发布）

- “素材管理 → 存储清理”现会在同一次扫描中审计 `static/image-generation-examples`，但固定案例目录继续使用独立严格边界，不会被普通生成目录的宽泛路径解析误处理。
- 后端同时读取全部本机模式记录、运行时任务和发布预设；当前案例、其它模式、发布预设、任务、草稿、画布、收藏仍引用的文件一律保护。任一 JSON 损坏、路径异常、链接/重解析点、哈希不符或扫描不完整时失败关闭。
- 固定案例候选只接受位于合法案例根目录、文件名为 64 位 SHA-256、内容哈希匹配且扫描期间大小/修改时间未变化的图片。
- 持久化 `storage-cleanup` 任务新增固定案例专用候选类型；确认前和文件暂存后都会重新扫描引用，新引用出现时立即回滚。最终只进入 Windows 回收站，失败恢复原文件并自动重试，不降级永久删除。
- 素材管理仍只有一个“存储清理”按钮；页面统计现包含案例文件总数、“旧案例残留”分类、候选标签以及当前案例/发布预设保护说明。
- 真实只读扫描结果：全部 `228` 个文件、占用 `792.3 MB`；生成图 `88` 个、临时上传图 `54` 个、案例文件 `86` 个；旧案例残留恰好 `3` 个，可释放 `5.8 MB`。当前 A2 的两张案例图片均受保护，未进入候选。
- 浏览器只执行扫描并退出确认步骤，没有点击“确认后台清理”，因此 3 个真实旧案例文件仍在原目录，未创建清理任务、未进入回收站；控制台 warning/error 为 `0`。
- 回归结果：存储清理专项 `35/35`、事务专项 `4/4`、素材管理 Node `3/3`、完整 Node `608/608`；完整 Python 唯一波动为既有 Windows 服务启动器临时端口停止测试超过 15 秒，该项单独复跑 `1/1` 通过（约 `13.9` 秒）。
- 交付前再次合并复跑存储清理与事务用例 `39/39` 通过；素材管理 Node `3/3`、Python 编译和 JavaScript 语法检查均通过。
- 3000 服务已在确认三类生成任务活动数和清理队列均为 `0` 后安全重启为 PID `33012` 并返回 HTTP `200`。本轮未读取或修改 `API/.env`、`data/asset_library.json`，未调用付费生成，未提交、推送或发布。

- 角色拆分确认：当前 83 张案例图包含 51 张示范输出图和 32 张示范输入/上传参考图；不是 83 张生成结果。输出图约 186.9 MiB，输入参考图约 37.3 MiB。

## 三十二、2026-09-04 本地模式与发布案例数量对账（当前未发布）

- 本机真实模式总数为 65：官方模式 51 个、本地新增模式 14 个；其中 64 个模式有示范，唯一空白的是 #63“菜品补齐”，其模式 JSON 当前没有 `example` 字段。
- 当前静态发布预设只导出官方 `builtin=true` 模式，所以安装案例目录的 51 张示范输出图只对应 51 个官方模式；本地新增的 13 个有示范模式并未丢失，它们的媒体仍在正式素材库中。
- 若以后要求安装包也包含本地新增模式，需要增加“选择哪些自定义模式随版本发布”的显式导出流程，避免把所有本地私人模式和素材默认打进安装包。本轮仅完成对账，没有改模式或图片。

## 三十三、2026-09-04 自定义模式纳入安装包的建议（待实现）

- 用户已确认希望安装包包含当前 13 个有示范的本地新增模式。推荐增加独立“随版本发布”标记/发布清单，不把自定义模式伪装成 `builtin=true`，避免未来官方种子更新覆盖本地模式。
- 导出阶段复制已选自定义模式的示范输入/输出图片为静态发布副本，并按约 1MP 等比例优化；正式素材库保留高分辨率母版。新安装按自定义模式身份导入，未选模式和任务/历史不随包发布。
- 发布前展示模式、示范完整性、原始/优化体积和预计节省；#63“菜品补齐”目前无 `example`，应显示缺失并阻止其作为完整示范发布。
- 本轮只完成发布链路分析和方案确认，尚未修改导出器、安装初始化、模式 JSON 或图片文件。

## 三十四、2026-09-04 官方模式发布边界修正（待实现）

- 用户确认自己是产品官方维护者，终端用户无权添加模式；前一节“自定义模式可选发布”的方案撤回。
- 正确目标为官方安装包包含当前全部 65 个模式，而不是只包含 51 个 `builtin=true` 模式。当前 64 个模式已有示范，#63“菜品补齐”作为官方模式保留，但应在发布预览中标记暂无示范。
- 需要把当前 #52–#65 的官方模式纳入官方种子/导出清单，保持稳定模式 ID 和已有任务引用，避免安装初始化重复创建；不增加终端用户的发布选择入口。
- 全部官方案例输入/输出图片统一按约 1MP 生成发布副本，正式素材库母版保留；本轮仅完成边界修正，尚未改导出器、安装初始化、模式 JSON 或图片文件。

## 三十五、2026-09-04 官方案例统一目录要求（待实现）

- 用户明确不区分“官方旧模式”和“新增模式”：现有 51 个及以后新增的模式全部是官方模式，均应进入同一个官方案例根目录和安装包。
- 统一目录建议为 `static/image-generation-examples/<稳定模式目录>/...`，每个模式独立子目录；输入参考图和输出示范图都由同一套发布、哈希校验和清理保护管理。
- 后续导出不再只按 `builtin=true` 过滤，而是遍历完整官方模式清单；历史 `builtin=false` 记录需要迁移为官方身份但保留稳定 ID 和任务引用。
- 没有示范的模式（如 #63）只发布模式定义，不创建空案例目录；设置示范时再生成优化后的案例文件，避免空目录累计。

## 三十六、2026-09-04 官方案例自动缩图与完整安装包（当前未发布）

- 新增 `image_generation_examples.py`：案例图片面积超过 `1,048,576` 像素时按比例缩小；不放大、不裁剪、不变形，保留格式、方向和透明通道，原始 CAS 生成/上传素材不改写。
- “设为模式示范”现在先生成并校验 `static/image-generation-examples/<稳定模式目录>/<SHA-256>.<ext>` 案例副本，再原子写入模式记录；优化失败时旧案例保持不变，页面显示“正在优化示范图”。
- 服务启动后异步执行可恢复的已有案例迁移，覆盖全部有案例模式，按内容哈希去重并记录 `data/image_generation_example_resize.json` 状态；失败模式自动重试，服务停止不会阻塞主页面。
- 官方预设导出不再以历史 `builtin` 标记过滤，当前模式目录中的官方模式全部进入清单；本次静态清单已导出 `65` 个模式，其中 `64` 个带示范，#63 仍保留为无示范模式。
- 安装候选包按清单只携带当前引用案例，旧大图只留在工作区并继续交给存储清理预览确认后移入 Windows 回收站，不自动删除。
- 新增缩图、原图保护、CAS 转静态案例、共享哈希去重、失败关闭、完整官方导出和 API 即时设置测试。

## 三十七、2026-09-04 官方案例自动缩图验收完成（当前未发布）

- 设置示范时即时按 `1,048,576` 像素上限生成静态案例副本；原始生成/上传素材保持不变。
- 后台迁移已处理全部 `64` 个现有案例，状态 `succeeded`，失败 `0`；#63 无案例模式跳过。
- 官方安装清单已包含 `65` 个模式、`64` 个案例和 `109` 个当前案例文件引用；旧大图只留在工作区，等待存储清理预览确认。
- 保存示范时会显示“正在优化示范图”，完成后提示最大实际尺寸；EXIF 方向、透明通道、比例和格式均有回归覆盖。
- 验证通过：相关 Python `229/229`、完整 Python `419/419`（4 项环境跳过）、完整 Node `609/609`，语法与差异检查通过。
- 本轮未执行真实清理、回收站移动、付费生成、提交、推送或发布。

## 三十八、2026-09-04 画布删除节点与生成记录自动对账（当前未发布）

- 删除无限画布中的上传/API/生成图片节点时，不增加额外二选一弹窗；经典画布和智能画布的整节点删除、结果清空、单张结果删除及批量删除均会自动提交删除对账。
- 新增 `POST /api/canvas-assets/reconcile-delete`，只定向处理 `source_type=canvas-online-image` 的历史。多结果记录只移除被删结果，最后一个结果移除后才删除整条历史，接口立即返回 HTTP `202`。
- 删除前必须先完成画布保存并确认候选图片已不在画布中；画布仍引用、历史关联不明确或引用扫描异常时失败关闭，不猜测删除。
- 相关媒体延迟约 5 秒交给现有持久化后台清理队列，为当前页面 Ctrl+Z 留出撤销窗口；后台再次扫描任务、画布、案例、收藏和历史等引用，只把真正无引用文件移入 Windows 回收站。
- 共享素材继续受保护：A1/A2 共用 B1 时，删除 A1 不会删除 B1；新生成结果保存 `canvas_task_id`，以后可更精确地关联来源历史。
- 撤销快照现包含画布生成日志；返回画布列表前会等待保存完成，并提交尚未发送的删除对账，减少快速离开页面造成的生命周期断层。
- 回归验证通过：Python 完整测试 `422` 项通过、`4` 项按环境跳过；Node 完整测试 `613/613`；JavaScript 语法与 `main.py` 编译检查通过。
- 服务已加载新代码并恢复为 PID `10208`：队列接口返回 HTTP `200`，新接口空候选验证返回 HTTP `202`、`cleanup_status=skipped`。本轮未使用真实用户图片执行删除或回收站验收，未调用付费生成，未提交、推送或发布。

## 三十九、2026-09-04 画布日志保留与存储清理口径修正

- 已按最终需求撤回“删除画布节点即对账并后台回收”的自动行为。经典画布、智能画布删除节点或结果时，只删除画布节点/连接并保存；`canvas.logs`、全局生成记录、提示词和时间信息全部保留，Ctrl+Z 快照仍可恢复节点和日志。
- `/api/canvas-assets/reconcile-delete` 已停用并返回 HTTP 410；历史 `canvas-reconcile` 任务载入后标记取消，不会再执行文件移动。
- 素材管理“存储清理”才是唯一物理清理入口。当前节点、运行/排队任务和其它业务引用继续保护；画布日志与 `canvas-online-image` 历史仅用于审计提示，不再单独阻止候选。确认后无当前引用文件才移入 Windows 回收站。
- 清理后日志记录继续保留；原图不存在时显示现有“文件缺失”状态，不保留额外缩略图。新增界面说明“画布日志仅作为历史记录保留，不代表当前仍占用图片”。
- 验证：画布删除与素材管理专项 Node 测试、存储清理 Python 测试及服务重启/队列接口检查通过；未执行真实用户文件清理、付费生图或 Git 发布。

## 四十、2026-09-04 侧栏基础功能分组调整

- 侧栏“本地功能”已更名为“基础功能”，折叠、展开、选中和状态记忆逻辑保持不变。
- “在线生图”和“GPT 对话”已归入基础功能折叠组；“一键主图”“一键详情页”“图片生成”“无限画布”保持独立入口。
- 在线生图和 GPT 对话仍使用原有页面 ID 与 iframe 路由，不影响页面功能或历史数据。
- 已补充 `nav.basicTools` 中英文文案，并通过侧栏结构专项测试（2/2）。

## 四十一、2026-09-04 全局备份入口移至更多设置上方

- 主壳层左侧底部新增“备份与恢复”，固定放在“更多设置”正上方；折叠态仅显示图标，展开或打开弹层时显示完整文案，不参与页面 active 高亮。
- 备份入口弹层复用侧栏设置弹层的定位、遮罩、阴影、键盘 Escape 和焦点恢复行为，提供“导出备份 / 导入备份”及简短说明；深色、窄屏和折叠状态沿用现有变量。
- 画布工作台右上角的重复备份按钮和下拉菜单已移除，原有备份弹窗、文件选择器及四个后端接口保持不变。
- 主壳层在需要时切换到项目工作台，通过同源 `studio-backup-action` 消息调用 iframe 内现有导出/导入流程；画布 iframe 尚未加载时保留待发送动作，加载完成后再发送，避免消息丢失。
- 增补侧栏顺序、唯一入口、同源校验和原有备份弹窗回归测试；定向测试 `23/23` 通过，i18n 校验 `1130` 个 key 通过，浏览器已验证两个入口均能打开原有流程。
# 2026-09-04 一键主图/详情页删除分组安全提速修复

- 修复正式素材位于 `assets/image-generation/media` 时被旧一键清理器判定“路径越界”的问题；清理任务现同时支持 CAS 媒体 ID、配套元数据 JSON、旧生成图和上传图。
- 主图和详情页删除分组只执行记录删除与持久化入队并返回 HTTP 202，不再因存储清理锁占用而等待；物理清理仍由后台单队列串行完成。
- 全局引用扫描已识别 canonical CAS URL，并保护主图、详情页、图片生成、当前画布节点、案例、收藏、草稿、运行任务和队列中的共享引用。
- 后台先完成稳定全量引用扫描，暂存后和回收前使用轻量签名复核；只有引用数据变化才重新全量扫描，避免固定执行三次约 6.5 秒扫描。
- 正式图片和元数据 JSON 必须成对进入 Windows 回收站；元数据缺失、哈希/路径异常、引用扫描不完整或文件变化时失败关闭，不使用永久删除。
- 旧失败任务自动转为 `review_required`，必须重新预览和二次确认；当前任务只读审计为 2 张生成图无引用、2 张上传图仍有引用，本轮没有移动真实文件。
- 验证：存储清理专项 46 项、Python 完整回归 430 项（4 项跳过）、Node 完整回归 614 项、JavaScript/Python 语法检查全部通过。

## 四十二、2026-09-04 一键主图/详情页单图删除安全回收

- “删除主图 / 删除分屏”现与整组删除使用同一安全后台回收体系：接口先完成记录事务并返回 HTTP `202`，页面立即移除对应卡片，不等待引用扫描或 Windows 回收站操作。
- 非最后一屏只移除该屏及其全部候选输出图，不把同组产品图和参考上传图加入候选；最后一屏会自动删除整个空分组，并按整组规则审计输入与输出素材。
- 新历史按 `source_type + source_task_id + source_screen_no` 精确归属；旧混合历史按输出 URL 与上游任务编号交叉核对，只裁剪目标屏图片，归属不明或历史损坏时失败关闭。
- 正式素材库 CAS 图片和元数据 JSON 成对进入后台事务；其它屏、其它分组、画布、案例、收藏、草稿或运行任务仍引用时保留，共享输入图与共享生成图均有回归覆盖。
- 新增持久化 `preparing` 状态：清理任务在任务 JSON、历史和内存状态提交前不可执行，提交完成后才原子切换为 `queued`。这消除了后台提前扫描旧引用后误判“仍占用”、最终留下孤立文件的低概率竞态；服务中断后可重新排队并完整审计。
- 前端两页统一增加单屏“删除中…”禁用状态、`202` 成功处理、最后一屏切换缓存下一组、后台清理中及失败重试提示；不使用整页刷新，也不会继续更新已删除屏幕。
- 验证通过：主图/详情页/存储清理 Python 专项 `96/96`，相关前端专项 `33/33`，完整 Python `440/440`（4 项环境跳过），完整 Node `615/615`，i18n `1130` keys，Python/JavaScript 语法和 `git diff --check` 均通过。
- 服务在三类生图活动任务为 `0`、清理任务文件为 `0` 时安全重启为 PID `56188`；主页、主图、详情页及三类任务接口均返回 HTTP `200`，浏览器只读验收控制台 error/warning 为 `0`。
- 本轮未点击真实删除、未移动用户图片到回收站、未调用付费生成，未提交、推送或发布。

## 四十三、2026-09-04 一键主图连续规划失败只读深查

- 对账截图中的 `#23–#27`：`#23/#24/#27` 为“产品图职责”校验失败，`#26` 为“参考图边界”校验失败，`#25` 为独立的上游连接断开；前四类失败均未进入图片生成阶段。
- 源产品图和参考图存在、静态地址 HTTP 200，附件顺序也与图1/图2角色一致，可排除素材缺失、URL失效和传图颠倒。
- 主因锁定为规划提示与本地校验契约错位：产品职责检查只搜索任意 `图N/Image N`，会拒绝语义正确但未编号的表达，也会让仅出现编号参考图、没有产品职责的文本误通过；参考边界检查同样依赖窄关键词。
- 自动修复实际会执行一次，但修复提示没有给出必须命中的固定模板；修复后再次使用等价表达便会终止。失败任务中的 `repair_calls=0` 和空 `parse_method` 是只在成功路径写回造成的诊断假象。
- 同一素材的历史成功组 `#10` 也曾依赖一次 repair 才通过，说明首次规划靠修复兜底早已存在；最近第三方兼容端点行为变化可能放大通过率波动，但不是唯一根因。
- 建议后续由程序确定性注入产品图职责和参考图边界、分角色校验编号与语义，并修正失败 trace；本轮只读诊断，没有修改业务代码、任务、历史、素材或服务，也未调用付费生成。

## 四十四、2026-09-04 一键主图规划契约修复与真实删除验收

- 主图规划现在按附件顺序由程序确定性注入产品图事实职责和设计参考图使用边界；产品图、参考图分别校验，创意正文独立检查长度、构图和禁止项，中英日韩及自定义语种均有稳定合同。
- planning、repair、解析方式、阶段错误及限长响应摘要在失败路径也会如实落盘，避免再次出现“实际修复过但记录显示 0 次”的诊断误导。
- 唯一授权的真实验收分组 `#28` 成功生成 2 张 2048×2048 主图：规划 1 次、修复 1 次、`strict_json`，未再出现“没有说明产品图职责/缺少参考图使用边界”。未触发第二组付费生成。
- 通过真实 Chrome 删除 `#28`：DELETE 返回 HTTP `202`，从确认到分组从页面消失约 `196.3 ms`，页面立即切换下一组且没有整页刷新。
- 后台清理完成后，任务 JSON、API 记录及所属历史均不存在；两张正式 PNG 与两份媒体元数据 JSON 成对进入 Windows 回收站，4 个回收站载荷大小与删除前一致；两张共享输入图保持原位。
- 真实验收额外发现成功任务记录移除快于前端轮询会产生一次 404。现为主图/详情页保留短时内存完成回执：最多 256 条、60 秒自动过期、服务重启清空，磁盘清理任务仍成功即删除，不会造成记录累计。
- 主图和详情页收到 `succeeded` 后均停止轮询并显示“图片清理完成”；真实浏览器终态模拟两页均为 0 error / 0 warning。
- 验证通过：主图任务及备份 20/20、存储清理专项 57/57、主图/删除前端专项 10/10、完整 Python 446/446（4 项环境跳过）、完整 Node 615/615，Python/JavaScript 语法和相关差异检查通过。
- 服务已在三类生图活动任务和清理队列均为 0 时安全重启为 PID `87756`；本轮未修改 `API/.env`、`data/asset_library.json`，未提交、推送或发布。

## 四十五、2026-09-05 发布候选白名单补齐（当前未发布）

- 接手审计发现原发布候选白名单落后于最近功能：候选树会漏掉详情页删除回收控制器/样式、智能画布日志保存控制器/入口、统一存储清理样式，以及画布对账、单屏删除和生命周期回归测试。
- 已同步修正 `tools/build-release-candidate.ps1` 与 `发布候选文件清单.md`；白名单现在包含 `74` 个路径，新增项覆盖详情页、智能画布、`i18n.js`、画布列表样式、统一清理测试和相关 Python 回归。
- 只做了静态候选审计，没有运行会改写工作区案例清单的导出/构造脚本；没有修改版本号、更新说明、`.env`、`data/asset_library.json`、任务、历史或媒体。
- 新鲜验证结果：PowerShell 脚本解析错误 `0`、白名单缺失 `0`、重复项 `0`；Python 编译通过；相关 Node 回归 `23/23`；相关 Python 回归 `31/31`；排除用户维护的 `static/runninghub/api_providers.json` 后 `git diff --check` 通过。
- 当前仍未暂存、提交、升版、构造最终候选包或发布；下一步应先确定发布版本号，再按更新后的白名单构造隔离候选，并对候选树重新做完整测试与敏感信息扫描。

## 四十六、2026-09-05 删除画布节点后孤立素材漏显示修复（当前未发布）

- 根因是旧版画布生成历史缺少 `source_type=canvas-online-image`：同一输出既存在于画布日志，又以普通 `type=online` 记录存在于 `history.json`，孤立计算误把后者当成图片生成模块强引用。
- 现在只识别画布日志 `outputs` 与历史输出字段的重复路径，并从旧版未标记历史的外部引用集合中扣除；历史参考输入以及主图、详情页、案例、收藏、草稿等其它真实引用不放宽。
- 当前真实接口由孤立素材 `0` 个恢复为 `14` 个；Chrome 页面已显示“孤立素材 14”、对应卡片及“未属于当前画布”详情。
- “碗”画布日志中的输出图和参考图文件当前均已不存在，继续遵守“文件仍存在才进入孤立素材”的既定口径，不伪造缺失文件卡片。
- 验证通过：画布资产专项 Python `8/8`、完整 Python `455/455`（4 项环境跳过）、完整 Node `616/616`、Python 编译检查通过。
- 服务已加载修复并运行于 PID `45028`，`/api/canvas-assets` HTTP `200`；本轮未修改画布、历史或用户素材，未执行真实删除、回收站移动、提交、推送或发布。

## 四十七、2026-09-05 图片生成官方备份分发发布

- GitHub `origin/my-custom` 已发布提交 `f2dbd1b`，版本为 `2026.09.05-custom.1`。
- 魔塔 `master` 已通过镜像提交 `be4f212` 同步；该提交以魔塔原有 `63159e8` 为父提交，树内容与 GitHub 提交完全一致，没有强制改写受保护分支历史。
- 本次发布精确上传 78 个程序、静态页面、测试、发布脚本和维护文档；公版只携带空图片生成种子，不携带官方案例图片。
- 明确排除 `API/.env`、`data/asset_library.json`、`assets/`、`output/`、`static/image-generation-examples/` 案例文件、RunningHub 本机配置、`.planning/`、审计草稿和同步辅助脚本。
- 发布前已验证图片生成 Python 回归、备份 Node 回归、Python/JavaScript/PowerShell 语法、候选构造和远端树一致性。
